bin/
/stella
/stellad
cover.out
coverage.out
coverage-*.*
coverage.html
/dist/
web/static/dist/
web/node_modules/
handoff.md
plan.md
/.plan/

# Generated code (produced by mise run generate)
api/types/gen.go
api/server/gen.go
internal/server/docs_spec.yaml
web/src/lib/api-client/

# Embedded tool binaries (downloaded via builddeps sync)
resources/binaries/binaries/*/*.gz

internal/agent/runner/.agents/
.chorus/

# Impeccable design-skill local caches and critique artifacts. config.json is
# the reviewable half — it holds the detector suppressions the whole repo shares,
# so a rule someone deliberately waived stays waived for everyone.
.impeccable/*
!.impeccable/config.json
web/.impeccable/

# Local credential files, never committed
.env*
__pycache__/
