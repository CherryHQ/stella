apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "stella.fullname" . }}-env
  labels:
    {{- include "stella.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.extraEnv }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
