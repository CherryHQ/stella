paths:
  /api/agents/{agentID}/tasks:
    parameters:
      - { name: agentID, in: path, required: true, schema: { type: string } }
    get:
      tags: [tasks]
      summary: List agent tasks
      operationId: listAgentTasks
      parameters:
        - name: status
          in: query
          required: false
          schema: { type: string }
      responses:
        "200":
          description: ok
          content:
            application/json:
              schema: {
                $ref: "../../components.yaml#/components/schemas/AgentTaskList",
              }
        "401": {
          $ref: "../../components.yaml#/components/responses/Unauthorized",
        }
    post:
      tags: [tasks]
      summary: Create an agent task
      operationId: createAgentTask
      requestBody:
        required: true
        content:
          application/json:
            schema: {
              $ref: "../../components.yaml#/components/schemas/AgentTaskInput",
            }
      responses:
        "201":
          description: created
          content:
            application/json:
              schema: {
                $ref: "../../components.yaml#/components/schemas/AgentTask",
              }
        "400": {
          $ref: "../../components.yaml#/components/responses/BadRequest",
        }
        "401": {
          $ref: "../../components.yaml#/components/responses/Unauthorized",
        }

  /api/agents/{agentID}/tasks/batch:
    parameters:
      - { name: agentID, in: path, required: true, schema: { type: string } }
    post:
      tags: [tasks]
      summary: Batch create agent tasks with intra-batch dependencies
      operationId: batchCreateAgentTasks
      requestBody:
        required: true
        content:
          application/json:
            schema: {
              $ref: "../../components.yaml#/components/schemas/AgentTaskBatchInput",
            }
      responses:
        "201":
          description: created
          content:
            application/json:
              schema: {
                $ref: "../../components.yaml#/components/schemas/AgentTaskList",
              }
        "400": {
          $ref: "../../components.yaml#/components/responses/BadRequest",
        }
        "401": {
          $ref: "../../components.yaml#/components/responses/Unauthorized",
        }

  /api/agents/{agentID}/tasks/unblocked:
    parameters:
      - { name: agentID, in: path, required: true, schema: { type: string } }
    get:
      tags: [tasks]
      summary: List unblocked agent tasks (all deps done)
      operationId: listUnblockedAgentTasks
      responses:
        "200":
          description: ok
          content:
            application/json:
              schema: {
                $ref: "../../components.yaml#/components/schemas/AgentTaskList",
              }
        "401": {
          $ref: "../../components.yaml#/components/responses/Unauthorized",
        }

  /api/agents/{agentID}/goals:
    parameters:
      - { name: agentID, in: path, required: true, schema: { type: string } }
    post:
      tags: [tasks]
      summary: Create a goal (parent task container)
      operationId: createGoal
      requestBody:
        required: true
        content:
          application/json:
            schema: {
              $ref: "../../components.yaml#/components/schemas/GoalInput",
            }
      responses:
        "201":
          description: created
          content:
            application/json:
              schema: {
                $ref: "../../components.yaml#/components/schemas/AgentTask",
              }
        "400": {
          $ref: "../../components.yaml#/components/responses/BadRequest",
        }
        "401": {
          $ref: "../../components.yaml#/components/responses/Unauthorized",
        }

  /api/agents/{agentID}/tasks/{taskID}:
    parameters:
      - { name: agentID, in: path, required: true, schema: { type: string } }
      - { name: taskID, in: path, required: true, schema: { type: string } }
    get:
      tags: [tasks]
      summary: Get an agent task
      operationId: getAgentTask
      responses:
        "200":
          description: ok
          content:
            application/json:
              schema: {
                $ref: "../../components.yaml#/components/schemas/AgentTask",
              }
        "401": {
          $ref: "../../components.yaml#/components/responses/Unauthorized",
        }
        "404": { $ref: "../../components.yaml#/components/responses/NotFound" }
    patch:
      tags: [tasks]
      summary: Update an agent task
      operationId: updateAgentTask
      requestBody:
        required: true
        content:
          application/json:
            schema: {
              $ref: "../../components.yaml#/components/schemas/AgentTaskUpdate",
            }
      responses:
        "200":
          description: ok
          content:
            application/json:
              schema: {
                $ref: "../../components.yaml#/components/schemas/AgentTask",
              }
        "400": {
          $ref: "../../components.yaml#/components/responses/BadRequest",
        }
        "401": {
          $ref: "../../components.yaml#/components/responses/Unauthorized",
        }
        "404": { $ref: "../../components.yaml#/components/responses/NotFound" }
    delete:
      tags: [tasks]
      summary: Delete an agent task
      operationId: deleteAgentTask
      responses:
        "204":
          description: deleted
        "401": {
          $ref: "../../components.yaml#/components/responses/Unauthorized",
        }
        "404": { $ref: "../../components.yaml#/components/responses/NotFound" }

  /api/agents/{agentID}/tasks/{taskID}/action:
    parameters:
      - { name: agentID, in: path, required: true, schema: { type: string } }
      - { name: taskID, in: path, required: true, schema: { type: string } }
    post:
      tags: [tasks]
      summary: Take an action on an agent task
      operationId: agentTaskAction
      requestBody:
        required: true
        content:
          application/json:
            schema: {
              $ref: "../../components.yaml#/components/schemas/AgentTaskAction",
            }
      responses:
        "200":
          description: ok
          content:
            application/json:
              schema: {
                $ref: "../../components.yaml#/components/schemas/AgentTask",
              }
        "400": {
          $ref: "../../components.yaml#/components/responses/BadRequest",
        }
        "401": {
          $ref: "../../components.yaml#/components/responses/Unauthorized",
        }
        "404": { $ref: "../../components.yaml#/components/responses/NotFound" }

  /api/agents/{agentID}/tasks/{taskID}/events:
    parameters:
      - { name: agentID, in: path, required: true, schema: { type: string } }
      - { name: taskID, in: path, required: true, schema: { type: string } }
    get:
      tags: [tasks]
      summary: List agent task events
      operationId: listAgentTaskEvents
      responses:
        "200":
          description: ok
          content:
            application/json:
              schema: {
                $ref: "../../components.yaml#/components/schemas/AgentTaskEventList",
              }
        "401": {
          $ref: "../../components.yaml#/components/responses/Unauthorized",
        }
        "404": { $ref: "../../components.yaml#/components/responses/NotFound" }

  /api/agents/{agentID}/tasks/{taskID}/deps:
    parameters:
      - { name: agentID, in: path, required: true, schema: { type: string } }
      - { name: taskID, in: path, required: true, schema: { type: string } }
    get:
      tags: [tasks]
      summary: Get task dependency info (upstream and downstream)
      operationId: getAgentTaskDeps
      responses:
        "200":
          description: ok
          content:
            application/json:
              schema: {
                $ref: "../../components.yaml#/components/schemas/AgentTaskDepsInfo",
              }
        "401": {
          $ref: "../../components.yaml#/components/responses/Unauthorized",
        }
        "404": { $ref: "../../components.yaml#/components/responses/NotFound" }
    post:
      tags: [tasks]
      summary: Add a dependency to a task
      operationId: addAgentTaskDep
      requestBody:
        required: true
        content:
          application/json:
            schema: {
              $ref: "../../components.yaml#/components/schemas/AgentTaskDepsInput",
            }
      responses:
        "200":
          description: ok
          content:
            application/json:
              schema: {
                $ref: "../../components.yaml#/components/schemas/AgentTask",
              }
        "400": {
          $ref: "../../components.yaml#/components/responses/BadRequest",
        }
        "401": {
          $ref: "../../components.yaml#/components/responses/Unauthorized",
        }
        "404": { $ref: "../../components.yaml#/components/responses/NotFound" }

  /api/agents/{agentID}/tasks/{taskID}/deps/{depID}:
    parameters:
      - { name: agentID, in: path, required: true, schema: { type: string } }
      - { name: taskID, in: path, required: true, schema: { type: string } }
      - { name: depID, in: path, required: true, schema: { type: string } }
    delete:
      tags: [tasks]
      summary: Remove a dependency from a task
      operationId: removeAgentTaskDep
      responses:
        "200":
          description: ok
          content:
            application/json:
              schema: {
                $ref: "../../components.yaml#/components/schemas/AgentTask",
              }
        "401": {
          $ref: "../../components.yaml#/components/responses/Unauthorized",
        }
        "404": { $ref: "../../components.yaml#/components/responses/NotFound" }

  /api/agents/{agentID}/tasks/{taskID}/runs:
    parameters:
      - { name: agentID, in: path, required: true, schema: { type: string } }
      - { name: taskID, in: path, required: true, schema: { type: string } }
    get:
      tags: [tasks]
      summary: List runs for a task
      operationId: listAgentTaskRuns
      responses:
        "200":
          description: ok
          content:
            application/json:
              schema: {
                $ref: "../../components.yaml#/components/schemas/AgentTaskRunList",
              }
        "401": {
          $ref: "../../components.yaml#/components/responses/Unauthorized",
        }
        "404": { $ref: "../../components.yaml#/components/responses/NotFound" }

  /api/agents/{agentID}/tasks/{taskID}/reviews:
    parameters:
      - { name: agentID, in: path, required: true, schema: { type: string } }
      - { name: taskID, in: path, required: true, schema: { type: string } }
    get:
      tags: [tasks]
      summary: List reviews for a task
      operationId: listAgentTaskReviews
      responses:
        "200":
          description: ok
          content:
            application/json:
              schema: {
                $ref: "../../components.yaml#/components/schemas/AgentTaskReviewList",
              }
        "401": {
          $ref: "../../components.yaml#/components/responses/Unauthorized",
        }
        "404": { $ref: "../../components.yaml#/components/responses/NotFound" }

  /api/agents/{agentID}/tasks/{taskID}/reviews/{reviewID}/decision:
    parameters:
      - { name: agentID, in: path, required: true, schema: { type: string } }
      - { name: taskID, in: path, required: true, schema: { type: string } }
      - { name: reviewID, in: path, required: true, schema: { type: string } }
    post:
      tags: [tasks]
      summary: Submit a review decision
      operationId: submitReviewDecision
      requestBody:
        required: true
        content:
          application/json:
            schema: {
              $ref: "../../components.yaml#/components/schemas/ReviewDecisionInput",
            }
      responses:
        "200":
          description: ok
          content:
            application/json:
              schema: {
                $ref: "../../components.yaml#/components/schemas/AgentTask",
              }
        "400": {
          $ref: "../../components.yaml#/components/responses/BadRequest",
        }
        "401": {
          $ref: "../../components.yaml#/components/responses/Unauthorized",
        }
        "404": { $ref: "../../components.yaml#/components/responses/NotFound" }

  /api/agents/{agentID}/tasks/{taskID}/criteria:
    parameters:
      - { name: agentID, in: path, required: true, schema: { type: string } }
      - { name: taskID, in: path, required: true, schema: { type: string } }
    get:
      tags: [tasks]
      summary: List acceptance criteria for a task
      operationId: listAgentTaskCriteria
      responses:
        "200":
          description: ok
          content:
            application/json:
              schema: {
                $ref: "../../components.yaml#/components/schemas/AgentTaskAcceptanceCriterionList",
              }
        "401": {
          $ref: "../../components.yaml#/components/responses/Unauthorized",
        }
        "404": { $ref: "../../components.yaml#/components/responses/NotFound" }
    post:
      tags: [tasks]
      summary: Add an acceptance criterion
      operationId: createAgentTaskCriterion
      requestBody:
        required: true
        content:
          application/json:
            schema: {
              $ref: "../../components.yaml#/components/schemas/AgentTaskAcceptanceCriterionInput",
            }
      responses:
        "201":
          description: created
          content:
            application/json:
              schema: {
                $ref: "../../components.yaml#/components/schemas/AgentTaskAcceptanceCriterion",
              }
        "400": {
          $ref: "../../components.yaml#/components/responses/BadRequest",
        }
        "401": {
          $ref: "../../components.yaml#/components/responses/Unauthorized",
        }
        "404": { $ref: "../../components.yaml#/components/responses/NotFound" }

  /api/agents/{agentID}/tasks/{taskID}/split:
    parameters:
      - { name: agentID, in: path, required: true, schema: { type: string } }
      - { name: taskID, in: path, required: true, schema: { type: string } }
    post:
      tags: [tasks]
      summary: Split a goal into child tasks
      operationId: splitGoalIntoTasks
      requestBody:
        required: true
        content:
          application/json:
            schema: {
              $ref: "../../components.yaml#/components/schemas/SplitTaskInput",
            }
      responses:
        "201":
          description: created
          content:
            application/json:
              schema: {
                $ref: "../../components.yaml#/components/schemas/AgentTaskList",
              }
        "400": {
          $ref: "../../components.yaml#/components/responses/BadRequest",
        }
        "401": {
          $ref: "../../components.yaml#/components/responses/Unauthorized",
        }
        "404": { $ref: "../../components.yaml#/components/responses/NotFound" }

  /api/agents/{agentID}/tasks/{taskID}/plan-ready:
    parameters:
      - { name: agentID, in: path, required: true, schema: { type: string } }
      - { name: taskID, in: path, required: true, schema: { type: string } }
    post:
      tags: [tasks]
      summary: Activate a goal and its draft children
      operationId: planReady
      responses:
        "200":
          description: ok
          content:
            application/json:
              schema: {
                $ref: "../../components.yaml#/components/schemas/AgentTask",
              }
        "400": {
          $ref: "../../components.yaml#/components/responses/BadRequest",
        }
        "401": {
          $ref: "../../components.yaml#/components/responses/Unauthorized",
        }
        "404": { $ref: "../../components.yaml#/components/responses/NotFound" }

  /api/agents/{agentID}/tasks/{taskID}/reopen:
    parameters:
      - { name: agentID, in: path, required: true, schema: { type: string } }
      - { name: taskID, in: path, required: true, schema: { type: string } }
    post:
      tags: [tasks]
      summary: Reopen a completed or failed task
      operationId: reopenAgentTask
      responses:
        "200":
          description: ok
          content:
            application/json:
              schema: {
                $ref: "../../components.yaml#/components/schemas/AgentTask",
              }
        "400": {
          $ref: "../../components.yaml#/components/responses/BadRequest",
        }
        "401": {
          $ref: "../../components.yaml#/components/responses/Unauthorized",
        }
        "404": { $ref: "../../components.yaml#/components/responses/NotFound" }
