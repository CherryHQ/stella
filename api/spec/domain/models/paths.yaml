paths:
  /api/models:
    get:
      tags: [models]
      summary: List enabled models from provider config and cache
      operationId: listModels
      responses:
        "200":
          description: ok
          content:
            application/json:
              schema: {
                $ref: "../../components.yaml#/components/schemas/CachedModelList",
              }
        "401": {
          $ref: "../../components.yaml#/components/responses/Unauthorized",
        }
