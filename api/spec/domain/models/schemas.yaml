openapi: 3.0.3
info:
  title: Models Components
  version: "1"
paths: {}
components:
  schemas:
    CachedModel:
      type: object
      required: [provider, model]
      properties:
        provider: { type: string }
        provider_name: { type: string }
        model: { type: string }
