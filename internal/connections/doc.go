// Package connections owns user-held OAuth connections: the bundles a user may
// connect, the flows that complete them, and the tokens the agent tools then
// spend. Connections are user-owned, so every use case binds one Authority.
package connections
