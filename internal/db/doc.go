// Package db owns PostgreSQL: pool construction, goose migrations, the
// embedded cluster used when no external server is configured, and the asset
// metadata for the embedded runtime tarball (pgruntime_asset.go).
package db
