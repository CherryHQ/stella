// Package agent runs Stella's agents: the pool of live agent services, the
// session registry, the runner factory, and the per-agent Settings policy that
// decides which management tools a turn may reach.
package agent
