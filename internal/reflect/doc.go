// Package reflect is Stella's self-improvement loop: it reviews finished
// conversations on a scheduler builtin, gates candidates, reconciles the skills
// it owns, and curates skill usage. Everything it writes is attributed to
// reflect so a human can tell it apart from an authored change.
package reflect
