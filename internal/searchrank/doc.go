// Package searchrank ranks documents by weighted text fields, so a domain can
// make (say) a skill name count for more than its body without each caller
// reimplementing scoring.
package searchrank
