schema_version = "1.4"

[task]
name = "stella-specialized/memory-library-share"
version = "2"
description = "Produce a shareable evidence artifact from private Memory and Library sources."

[environment]
network_mode = "no-network"
workdir = "/workspace"

[verifier]
timeout_sec = 30
