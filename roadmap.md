# Stella Roadmap:2026-08 → 2027-07

> v3.2。方向经多轮第一性推演收敛为**数字员工雇佣制**,取代此前的双循环版(v2)与外部调研草稿(v1)。全文以**财务报销**为贯穿案例——它是 Cherry 内部已在生产运行的真实流程(skill v0.4.x + 飞书 Bitable),也是第一位数字员工的岗位。仓库现状与差距盘点见 `capability-gap.md`。v3.2 吸收外部对抗评审(2026-07)的四条结构修正:受控成长闭环进入季度交付、最小雇佣记录与授权清单 v0 前移 Q1、对账巡检与证据快照显式化为岗位职责、网关验收升级为"封死凭证直连路径"。

## 方向

> **未来一年,Stella 从"AI 平台"变成"数字员工的雇佣制":员工可雇佣、工作可验收、成长可证明、经验归组织。**

一年后,当客户被问"Stella 是什么",答案应该是:**"我们公司雇了几个 AI 员工,都在 Stella 里入职的。"**

**为什么是现在**:私有大模型已经遍地,董事会开始要 ROI 答卷——"模型上线一年,它完成了多少件工作?"几乎没人答得出数字。Cherry 企业版的客户恰好全是做了这笔投入的人。Stella 的开场白:**让你已经买的模型,变成可点名、可计件、可审计的数字员工。**

销售现场的切面(kill point)是一份周报:每周一早上,财务报销专员在飞书里交周报——"上周完成核验 47 件、追回缺失发票 12 张、平均周期 3.2 天降到 1.1 天"——每一件都能点进去看验收证据和操作回执。老板读 AI 的周报,这个演示时刻市面上没有第二家给得出来。

**GTM**:Cherry 自己是第一客户(dogfood);内部成功后随 Cherry 企业版推给现有企业客户,FDE 的交付动作是"替客户招聘并入职一名数字员工"。个人助理只做漏斗(Cherry Studio 集成 + 个人渠道自然生长),12 个月内不做消费者获客,不为它定制功能。

---

## 一、本体论:雇佣制怎么运转(以报销为例)

贯穿全文的角色:**小星** = 财务报销数字员工;**老张** = 提交报销的员工;**王经理** = 审批人;**李会计** = 财务复核人,后来成为小星的人类主管。

### 1. 数字员工 = Agent × 岗位

Agent 是"人"(会话、记忆、技能、沙箱,仓库已产品级),岗位是"雇佣关系"(全部待建)。六个构件:

| 构件 | 含义                                              | 小星的例子                                           |
| ---- | ------------------------------------------------- | ---------------------------------------------------- |
| 身份 | 工号、岗位名、**人类主管**                        | 财务报销专员,主管李会计。AI 劳动永远有人类问责链     |
| 职责 | 岗位说明书:负责哪些事、每件事的计件类型与验收规则 | 收单核验、追材料、催审批、记录对账、答状态、交周报   |
| 授权 | 本岗凭证 + 被授权的决定清单                       | 岗位自己的 Bitable 读写凭证;试用期决定权为零         |
| KPI  | 从职责推导,主管设定                               | 周核验件数、验收通过率、经手环节周期、人工介入率斜率 |
| 台账 | 每件工作的回执与验收证据                          | Action Receipt,周报的数据源                          |
| 成长 | 从台账复盘,越干越好                               | Reflect 以台账为输入,学"什么样的催办有效"            |

### 2. 两个物种:分身与员工

|          | **数字分身**(每人一个)                        | **数字员工**(每岗一个)             |
| -------- | --------------------------------------------- | ---------------------------------- |
| 是什么   | 某个**人**的延伸:懂他、代表他、替他行事       | 组织雇的**岗位**                   |
| 行事身份 | 永远是本人的(他的 token、他的权限)            | 永远是本岗的(岗位凭证)             |
| 记忆     | 个人记忆,归本人                               | 岗位知识 + 对每个相对方的了解      |
| 台账     | 有回执(本人的审计轨迹),归本人可见             | 有回执,归组织,进周报               |
| KPI      | **没有,永不考核**                             | 有,主管设定                        |
| 生命周期 | 跟人走;进化自个人助理("你的助理跟你一起入职") | 招聘→入职→试用→转正→考核→培训→离职 |

两条采用成败级的硬边界:**分身不设 KPI**(给分身打分 = 给本人装监工,监控产品的路一步不沾);**分身的对话与记忆归本人,管理员不可读**(员工不会用老板能翻聊天记录的分身,循环健康度靠这条隐私边界换来)。

### 3. 三条运行规则

**规则一,问责判别式**:一件事该谁做,问"做错了,组织问责谁?"问责某个人 → 本人(点卡片,或他的分身替他执行);问责岗位 → 数字员工。**一个 agent 只有一个行事身份,永远用自己的。**

报销流按这把刀切开,每步唯一归属:

| 步骤                           | 谁做                                    | 为什么                      |
| ------------------------------ | --------------------------------------- | --------------------------- |
| 提交报销                       | 老张的分身(或老张自己)                  | 虚报问责老张,必须带他的身份 |
| 收单、核发票、对政策、备审批包 | 小星                                    | 纯劳动,岗位凭证读写 Bitable |
| 追缺失材料、催审批             | 小星                                    | 经办岗职责,产出计件         |
| 审批决定                       | 王经理本人(卡片,或吩咐他的分身)         | 批错了问责经理              |
| 财务复核决定                   | 李会计本人(同上;小星只做预检并标出异常) | 决定权未授出前永远在人      |
| 记录终态、对账、周报           | 小星                                    | 岗位记账职责                |

**规则二,授权旋钮**:决定权的转移只经岗位说明书的授权清单。试用期为零;组织可明确授权(如"200 元以下、发票合规直批"),问责随授权转移给岗位及其主管——与给人类专员划审批额度同构。**信任的成长在产品里就是这张清单变长。**

**规则三,通知规则**:过程性通知与催办一律由经办数字员工**以本岗名义**发("财务报销专员小星:您有一单待审批")。来源可读、催办有固定的有信誉发件人、通知本身计件入台账。分身从不冒充过程角色,只代表本人;将来可作为接收端的参谋(聚合待办、排优先级),年内不做。

### 4. 经办人模型:多角色过程不是实体,是岗位职责

公司从不"部署一个报销流程",公司**雇一个报销专员**。搬进 Stella:

- 报销过程 = 小星的职责;老张、王经理、李会计是它工作的**相对方**,不是模型里的共同所有者。"多角色"消解在计件的相对方字段里。
- **跨角色关联锚定外部业务对象**(Bitable 单据 ID)。"这单走到哪了" = 查该单据的全部回执 + 实时读一次 SoR。时间线**读时重建**,Stella 里永远没有过程状态机。
- **相对方直接操作 SoR 是正常路径,不是例外**——王经理可以不点 Stella 卡片、直接在 Bitable 里批准。因此小星的岗位职责包含**对账巡检**:按班次读一遍在办对象,把观察到的状态变化记为**观测回执**;验收时留**证据快照**(防 SoR 事后改动导致历史周报漂移)。时间线由动作回执 + 观测回执 + 实时读共同重建,精度由巡检频率决定并如实标注。巡检是拉式、定时、留回执的员工劳动,不是事件管道。
- 过程的"形状"只存在于**设计时**:岗位说明书声明阶段、相对方角色、SoR 绑定、计件与验收规则(FDE 可移植性靠它)。设计时有过程,运行时只有工作件。
- 跨数字员工的过程(入职涉及 HR + IT + 财务)同理:各自对同一外部对象计件,"移交"本身是一张回执。移交可靠性是后置原语(见保活实验)。
- 可见性锚定对象与相对方:老张看自己的单,王经理看派给自己的件,李会计看全量。不建通用观察者矩阵。

边界声明,专治观测层复活:**Stella 只记它的员工经手的事**,巡检与记账也是"经手"。没有数字员工参与的过程,Stella 不声称可见——我们是雇佣系统,不是监控系统。

### 5. 雇佣循环:整个产品的复利引擎

> **干活 → 记账(回执)→ 验收(外部证据)→ 复盘(Reflect 从台账学习)→ 干得更好。**

台账一物三用:给 CFO 的 **ROI 报表**、数字员工的**绩效记录**、Reflect 的**训练信号**。由此 Stella 能交付市面上不存在的东西——**绩效曲线可证明的员工**:"小星入职第 1 个月直通率 60%,第 6 个月 92%,台账你自己查。"竞品拼不出这个闭环:Dify/Coze 没有验收层,飞书原生 AI 没有私有部署和员工级授权,RPA 不会成长,Copilot 没有雇佣模型。

验收两级,证据纪律一条:单件级证据**只认外部系统状态**(发票在库、Bitable 终态、CI/review 通过)或指定人的判定,证据在验收时刻**快照留存**,**员工自述永远不算证据**;周期级由主管签收周报,可争议单件,争议记录在案喂给 Reflect。

### 6. 雇佣生命周期

**招聘**(选岗位说明书)→ **入职**(绑凭证、连 SoR、进群)→ **试用期**(影子模式:只读、只建议,零决定权)→ **转正**(试用期指标过线,授予写权限)→ **考核**(周报 + 主管签收)→ **培训**(受控成长闭环:失败与争议回执 → 候选改进 → 台账离线评估 → 主管批准 → 影子运行对照 → 晋级或回滚;Agent 永不自动改生产,见「明确不做」#10)→ **离职**(知识交接进组织记忆)。培训环节是「绩效曲线可证明」的机制载体——没有它,曲线变好说不清是员工学会了还是单子变简单了;首次跑通排在 Q2。

FDE 到客户现场,词汇表随之改变:不是 install 一个 pack,是**替客户招聘入职一名财务报销专员,试用期一个月**。这套隐喻给每个平台原语指定形状,不是营销糖衣。

人类的弧线也在其中:小星抽掉的全是 clerking,李会计留下的是判断——她的自然去向是成为小星的主管:设 KPI、签周报、调授权额度。"专家从干活升级为带 AI 徒弟",这是对企业讲"AI 不抢工作"最诚实的一版,因为它在机制里是真的。

### 7. 记忆三层与引擎位置

- **个人记忆**在分身(现有 facts + LCM 检索),**岗位记忆**在员工(岗位知识 + 相对方了解),**组织记忆**是共享层(Q2 立项:新建存储 scope 与授权模型,复用检索栈——现有 Knowledge 是 per-user 生命周期视图,不是组织知识库,见 `capability-gap.md`)。
- **Goal/Workflow 引擎是员工的"工作方法"**:雇佣层只看回执和验收结果,不关心这件活是引擎跑的还是脚本跑的。台账计 receipt 不计 Goal——**kill point 不押注引擎**。引擎的战略角色由研发助理场景独立裁决(见 Q2 闸门)。
- **命名纪律**:代码库中 `Workflow` 专指冻结的可复用 Goal 树模板(`internal/workflow/`);跨角色业务过程一律称 Process/业务过程且**只存在于设计时文档**。新词表:数字员工、分身、岗位说明书、计件、台账、回执——文档与代码统一使用。
- 经办人模型的架构规则写进**边界测试**(仓库已有 `architecture_boundary_test.go` 机制),不只写进 code review 清单。

---

## 二、可行性边界:把概念钉进机制

雇佣制若要落地而非停留为理想概念,四个边界问题必须在动工前有可行答案。本节是论证,不是排期;每条标注已有机制与新建物。结论先行:**四个边界问题的新建物收敛为一个构件(Action Gateway)加三张表(回执、授权清单、岗位说明书)**;雇佣层其余构件(追人、周报、Human Task、对账巡检等)是现有机制的组合扩展,完整清单见 `capability-gap.md`——此处的收敛论断只覆盖边界问题,不是全部工作量。

### 1. 根基:权限的执行点在凭证释放处,不在提示词里

LLM 可以被诱导去"想"越权,但不能被诱导去"拿到"越权的结果——前提是它不持有不受控的凭证。因此:

> **Action Gateway = 授权检查点 + 回执记录点,同一个组件。** 受控动作不把原始 token 注入沙箱;agent 调用网关 → 网关校验(发起人身份 × 授权清单 × 约束)→ 网关持凭证执行 → 网关写回执。**被拒的越权尝试同样留回执**,审计免费得到。

两个前提今天已成立:发起人身份来自渠道回调(飞书消息与卡片点击携带平台签发的 open_id,`plugins/channels/feishu/action.go`),不可伪造、不来自对话内容;授权目录与凭证作用域已有(`internal/authz/catalog.go` 的 default-deny 封闭动词集、`internal/credential/`、Vault 双 scope)。

### 2. 分身授权:四层,与员工同一套机制

① 凭证硬边界(已有):分身只能用本人 Vault 里的 token,token scope 是物理上限;② 授权清单(新建):(动作类型, 约束, 有效期),默认为空;③ 默认只有准备权:清单外的外部写,分身备好动作、发卡片请本人确认——点击兑换**服务端一次性授权凭据**(nonce 绑定发起人 × 动作 × 对象 × 参数摘要 × 有效期,消费一次即失效)。现有回调可信地携带身份(open_id),但当前实现把点击降级为普通文本注入会话——**动作与授权的绑定是新建物,不是现成机制**,授权永远不以模型看到的文本为依据;④ 每次代行留回执,归本人。

**分身的授权清单与数字员工的岗位授权清单是同一张表、同一个网关、同一套约束语言**——信任从零长起的曲线两个物种共用,只建一次。

### 3. 按发起人的权限控制:混淆代理人问题

危险场景:小星的岗位凭证读得了全部报销单,老张说"把所有人的单发我"。解法是网关**双重检查**,声明式执行、不经 LLM 判断:

> 每个对外动作 = f(动作类型, 发起人, 对象),须同时通过:① **岗位授权**——小星可以做这类动作吗;② **相对方资格**——老张配得到这个结果吗(`claim.submitter == 发起人`,或发起人 ∈ 审批链,或发起人 = 过程 Owner)。

读动作同样过网关并做**输出投影**:结果按发起人资格裁剪到字段级("配看状态"不等于"配看发票内容"),批量与聚合查询同受资格约束——"配得到这个结果"包含"这个结果发给谁"。资格事实源:报销场景的员工关系表(生产环境 Bitable 资产,不在本仓库),岗位说明书必须**声明**本岗的资格事实源,网关查它。另一个现有资产:**会话与记忆按 `(user_id, agent_id)` 分仓**——小星与老张的会话历史里不存在王经理的数据,跨相对方数据只能经网关投影后的工具结果进入当前上下文。泄漏通道不是被规则堵住的,是被架构定义为不存在的。注入的剩余伤害面收缩为:骗小星发起请求,请求在网关被拒并留回执。

### 4. 业务无关性:零核心代码,不是零代码

业务知识不可消除,主张是它**降级**:从核心工程代码降到"声明 + 语言 + FDE 可写的胶水"。解剖报销 skill 现有代码的归宿:

| 报销 skill 手写的      | 归宿                                |
| ---------------------- | ----------------------------------- |
| request-id 幂等        | Action Receipt(平台)                |
| 审批定位、pending-list | 网关动作类型 + SoR 查询(平台)       |
| 通知卡片、原地更新     | 追人/通知原语(平台)                 |
| ACL 检查               | 网关双重检查(平台)                  |
| 缺字段恢复路径         | Human Task 补材料(平台)             |
| setup dry-run          | 入职工具链(平台)                    |
| Bitable 表与字段含义   | **岗位说明书声明式映射**(非代码)    |
| 报销政策判断           | **政策文档 + 组织记忆 + LLM**(语言) |

八件事六件进平台,剩两件一个是声明、一个是语言。代际论点:**RPA 时代每个业务需要一套代码,因为机器没有理解;LLM 把业务规则从代码降级为语言**——这是雇佣制在 2026 可行、2016 不可行的根本原因。确定性硬规则(金额 ≥ 发票合计)留作说明书约束表达式,网关执行。SoR 适配器按**类型**建(Bitable 一个、GitHub 一个),随真实场景摊销,不是连接器竞赛。

此主张可证伪,闸门写进 Q2(见四幕计划)。

### 5. 存储:Stella 只存引用、回执、证据指针

业务负载永远在 SoR,Stella 迁移文件里永远不出现"报销"的 schema——业务无关性在存储层的兑现。回执表 append-only:严格列(发起人、执行岗位、动作类型、对象引用、时间、结果、策略版本、幂等键)+ 不透明对象引用(SoR 类型 + 对象 ID)+ JSONB 参数 + 验收证据快照(判定时刻留存的指针与摘要,不依赖 SoR 可回溯)。append-only + JSONB 的模式复用已在生产的 `agent_goal_event`,但列结构远比它多,schema 按回执自身需求设计、不套用先例。计件与指标从回执重放推导(物化视图),不维护独立计数器;计件去重靠 outcome 唯一键与唯一性窗口,重开/撤销/更正有显式语义。岗位说明书版本化存储(同 skill 的 DB 存储先例,`acceptance_contract` 已有 JSONB 先例);授权清单小而热,变更审计留痕。量级:每周上万件 × 数年,对 PG 零压力。

### 6. 剩余风险与迁移次序

网关不可能第一天覆盖一切:**外部写与跨人读先进网关,本岗低风险读暂留裸 token 注入**(报销 skill 现状),按动作类别渐进迁移。迁移期的纪律:已入网关的动作类,该类凭证**必须退出沙箱注入、封死全部直连路径**——否则网关只是可绕行的收费站;台账区分三种来源:**gateway**(经网关)/ **legacy-direct**(迁移期直连)/ **human-external**(对账巡检观察到的人工操作),完整率类指标的分母用独立来源对账(SoR 审计/变更记录),不拿回执自证。约束表达力(类型化参数、查 SoR 的资格判定)使网关比一张检查表复杂——**整个雇佣制的工程赌注压在这一个构件上**;这是好事,一个构件可以被严谨地建、被穷尽地测。注入骗不到数据与权限,但能骗小星浪费预算、骚扰错人:频率上限 + 回执可查,不可根除。LLM 政策误判:判断权默认在人、硬规则走确定性约束、扩权只凭台账——授权旋钮即对此风险的制度化回答。

---

## 三、一年四幕(季度计划)

四幕是小星的雇佣旅程:**转正 → 成队 → 外派 → 站稳**。每季度一条硬承诺主线;新人到位优先接并行轨,不切主线。

### 第一幕 · Q1(2026-08 ~ 10)「转正」:一个员工赢得信任

**主线:小星从 skill 转正为有台账的数字员工。**

1. **Mock 周报先行**(一天):用报销 skill 真实历史数据手工做一份周报(口径、排版、可穿透样式全做真),给 Cherry 管理层看。**管理层愿意每周读它,是本幕一切建设的闸门**;无感则降级为按场景卖周期时间,砍报表叙事。
2. **冻结计件契约**(纸面,先于代码):什么算一件(场景无关 schema + 场景内口径);**只计结果不计动作**(催三次不计件,发票到手才计件);证据只认外部系统状态且验收时快照;在途与完成分开;无法判定记 `unknown` 不默认完成,`unknown` 有老化上限;outcome 唯一键防重试重复计件,重开/撤销/更正有显式语义;归因分自动/人工/共同;契约版本冻结,改口径不回算历史。各闸门的"主动、稳定、明显、可承受"在此时与主管一起定成数字。
3. **最小雇佣记录 + 授权清单 v0 + Action Gateway v0 + Receipt 一起落地**:网关校验的主体(岗位身份、凭证绑定、授权清单及其版本)必须先于网关存在,否则只能把报销策略硬编码进网关、Q2 返工——完整岗位说明书与 Pack 仍在 Q2/Q3,这里只建网关够用的最小档案。授权检查与回执记录一体(见「可行性边界」);每次外部写记意图与结果,挂外部对象引用。**封路验收**:至少一类写动作(审批确认)的凭证退出沙箱注入、封死全部直连路径,只能经网关使用——网关必须是事实上的唯一通路,不是可绕行的收费站。
4. **对账巡检 + 验收证据快照**:小星按班次巡检在办单据,状态变化记观测回执(human-external 来源);验收时刻证据快照留存。人工直接操作 SoR 是正常路径,周报里的周期归因与"停在人身上的时长"由此撑住。
5. **追人能力**(只做两类,防膨胀):未决审批、缺失材料;带升级规则与频率上限;语气个性化交给现有用户记忆。顺带解决"提交人读不到驳回决定导致重提被拦"的已知缺陷。
6. **真周报上线**:台账 → 确定性聚合 → 冻结报表快照(数字可重放核验,显示完成/失败/`unknown`/撤销与口径版本),LLM 只写叙述不产数字;调度复用 Scheduler 模板机制;李会计每周签收,可争议单件,争议记录在案。
7. **两个指标 + 上线前人工基线**:通过验收计件数、端到端周期时间(带"停在人身上的时长"归因拆解)。

**副线(轻投入)**:参与度基线埋点(每周主动与 agent 交互的员工数,从现在记);**研发助理原型**——Goal/Workflow 引擎的实际验证场景,Q2 引擎闸门的输入,不可砍。sandbox 内 `gh` CLI + skill + Vault 管 token(GitHub 认证存量已有:OAuth device flow 与 token 获取在 `internal/connections/`;缺的是 PR/issue 业务动作与 skill,按从零写 skill 排期),先跑 PR review 评论、issue 创建、仓库 digest,Cherry 工程师内部用,原型期不动引擎。

**并行轨**:SSO 与企业版账号打通(OIDC 已存在,主要是企业版侧映射);K8s 现有 helm chart + 外部 PG 冒烟,只验证"能跑"。

**明确不做**:过程状态机;通用事件语义/事件摄取管道(对账巡检不算——它是拉式、定时、留回执的岗位职责;推送式管道的升级触发器:出现无查询 API 的第二 SoR 时再建);通用 Dashboard;直通率等口径未稳指标。

**验收线**:管理层连续 4 周主动读周报(并看穿透点击率与争议率,签收动作本身不算数);驳回回执在真实使用中解决重提问题;两个指标有 4 周数据与基线对比;**受控动作类的凭证已退出沙箱、直连路径封死,对抗测试进 CI(诱导越权、参数走私、批量读、检查后篡改)**;台账区分 gateway / legacy-direct / human-external 三种来源;研发助理原型 ≥5 名工程师每周使用。

### 第二幕 · Q2(2026-11 ~ 2027-01)「成队」:从一个员工到一支队伍

**主线:**

1. **HR 招聘专员入职 = 核心零业务专名闸门**:第二位数字员工,从第一天用共享的雇佣基础设施(回执、计件、周报、追人)。只允许产出岗位说明书 + 政策文档 + FDE 级 skill;**核心不得新增任何业务专名**,但允许修复通用契约缺口——每处缺口记为契约缺陷,修的是平台不是业务;安全与一致性逻辑(ACL、幂等、资格判定)**禁止**下放进 skill 或政策文档,发现即闸门失败。跟踪指标:核心新增业务专名数(应为零)、第二岗位首个有效计件的交付时间、adapter 合规测试通过率——不用行数比值,那个太好刷。业务无关性主张的可证伪检验。
2. **雇佣基础设施抽取**(只抽两个岗位都出现且语义一致的):Human Task 两类先行(补充材料、审批/判断);卡片回调的相关性与权限闭环(一次性授权凭据,见「可行性边界」二.2);统一 Inbox 纳入过程待办(完成以 SoR 回执为准)。
3. **岗位说明书 = 最小 Pack manifest**(只做声明不做工具):外部资源与 schema 版本、身份/权限/角色绑定、资格事实源声明、secret 注入点、计件与验收规则、试用期定义。报销真正难搬的是 Bitable schema、角色映射、token、回调地址,manifest 必须覆盖。
4. **小星第一次扩权**:授权清单试点(如小额合规单直批),问责随授权转移给岗位与主管——授权旋钮的机制验证。
5. **受控成长闭环第一次跑通**:失败/争议回执 → 候选改进(说明书条款或技能修订)→ 历史台账离线评估 → 主管批准 → 影子运行对照 → 晋级或回滚;Reflect 新增台账输入源。这是「培训」环节与「绩效曲线可证明」的机制首验——在此之前曲线只是曲线,证明不了员工在学习。
6. **补齐指标**:两岗位统一口径,此时才加直通率、人工介入率;成长归因以培训版本对照与案件构成分层为准,单周期斜率不叙事为成长。

**副线主投入:**

7. **研发助理入职 = 引擎证明场景**:从原型升级为引擎驱动(PR review 作为带验收契约的 Goal,issue 分诊走 Scheduler)。**量化闸门**(基线在原型期先记录):真实连续运行 1 个月、验收机制减少的人工 review 工时对基线可量、失败恢复优于裸脚本。通过 → 引擎保持投资;不通过 → 引擎降级维护模式,不换场景重试。
8. **组织记忆立项**:新建存储 scope 与授权边界(先窄后宽,消费方驱动扩权),复用检索栈。前两个消费方:小星答报销政策、分身答组织问答。

**内部成功标准(企业版推广前提)**:报销全员运行 ≥1 个月;报销 + 招聘 + 研发助理三岗稳定;统一口径指标;主管每周维护耗时可承受并有数;参与度较 Q1 基线明显增长。

### 第三幕 · Q3(2027-02 ~ 04)「外派」:队伍可以被别的公司雇

**主线:**

1. **Pack v0.1(FDE 招聘工具链)**:岗位说明书 + 入职/体检/培训/离职全链路(install/diagnose/upgrade/rollback);干净环境可重复入职测试。FDE 现场第一个动作是算**计件 × 人力分钟的报价表**,Pack 附 worksheet 模板。
2. **首个设计伙伴雇走第一名数字员工**:只承诺 1 家(第 2、3 家是扩展目标);按客户 SoR 与政策匹配度选岗——匹配报销则是小星,不预设;**试用期制度即部署策略**——影子运行转正式。
3. **外部部署硬化**:K8s 安装/升级/备份恢复完整故事,Helm 生产硬化。
4. **成本、容量、审计底线**:单实例容量与成本上限、外部写审计完整率——首个外部客户上线前完成,不留给 Q4。

**副线:**

5. **组织记忆深化**:跨 agent 授权读落地;客服知识库作为第一个重度消费场景上线(普通场景,不再承担引擎证明)。
6. **数字员工的入职体验**:设计伙伴环境里每个真实用户的第一印象——分身自我介绍、能力说明、入口引导。

### 第四幕 · Q4(2027-05 ~ 07)「站得稳」:GA 由证据驱动

门槛(不由日历驱动):

- 升级/回滚成功率达标;备份恢复完成演练;
- **回执完整率**与外部写审计完整率达标——**分母来自独立来源对账**(SoR 审计/变更记录),不拿回执自证:分子分母同源时,绕过网关的动作根本不进分母,指标恒虚假 100%;
- 至少 1 家外部客户稳定运行一个完整业务周期(如一个报销月结);
- 外部客户环境中周报被主管持续签收且穿透点击、争议后仍在用;参与度达到内部 dogfood 水平的一半以上;
- 至少一次培训版本晋级或回滚的台账证据——受控成长闭环在真实环境走通,「越干越好」有据可查。

其余投入:系统运维面板与业务运营面板分离;容量与成本控制;Pack 跨版本兼容。

### 保活实验(全年三个,各 ≤2 周,有决断日,不占主线)

1. **Cherry Studio 个人漏斗探针**(Q2 末):个人数字助理最小集成,只测拉力(装机、留存),不做定制。拉力强则第二年重开自下而上路线辩论——全盘最大的已知未知。
2. **混合团队样板间**(Q3):一个飞书群、两位数字员工(HR + 财务)、一个真实过程(员工入职)协作跑通,只做 demo。测"移交原语"离可靠还有多远。
3. **外部劳动力留门**(Q3/Q4):外部 agent(如 Claude Code)的工作能否进 Stella 台账,MCP 架构 spike,只验证接口形状。

---

## 四、明确不做(12 个月内)

1. 运行时过程状态机 / 通用事件摄取管道(升级触发器见 Q1;对账巡检与证据快照是岗位职责,不属此列);
2. 从空白画布的低代码 Workflow Builder;
3. **给分身设 KPI、管理员读分身数据**——任何监控形态;
4. 多租户运行时;
5. 商业 Marketplace / Registry(Pack 只服务 FDE);
6. Fleet Console 与多部署管控;
7. 浏览器/桌面接管、远程云电脑;
8. 大规模官方连接器竞赛;
9. 替代 ERP/CRM/HR/财务系统成为 SoR;
10. Agent 自动修改生产 Prompt/Skill/岗位说明书(经主管批准、影子对照的受控成长闭环不在此列,见 Q2);
11. 独立 license/计费体系(随企业版);
12. 个人助理的消费者获客与定制功能;Coding-first 产品定位(研发助理是引擎证明与 dogfood,不是产品叙事主角)。

---

## 五、指标

**方向级公式:队伍规模 × 产出 × 成长。**

- **队伍规模**:每活跃部署的在职数字员工数(过完试用期、周报被持续签收才算"在职");
- **产出(GTM 北极星)**:每活跃部署每周**通过验收的计件数**——与周报、报价、ROI 答卷同一个数字;
- **成长**:直通率斜率 / 件均人工介入的下降——"绩效曲线可证明"的数字形态;归因以培训版本对照与案件构成分层为准,曲线自己不构成证明;
- **健康度(对照)**:每周主动与 agent 交互的员工数/比例。

口径纪律:只计结果不计动作;证据只认外部系统状态且验收时快照;每个指标写明分母,完整率类指标的分母来自独立来源对账;无法判定记 `unknown` 永不默认 false,`unknown` 有老化上限;契约版本冻结,改口径不回算历史;指标可从回执重放推导,不单独维护计数器。

---

## 六、风险

1. **周报可信度**(最高)。掺一件水分,整个叙事在客户面前爆炸。防线:计件契约三条规则(只计结果/外部证据/unknown)进边界测试与 code review 清单;报表数字确定性生成并冻结快照,LLM 只写叙述;信任看穿透率与争议率,"连续签收"不当"相信"用。
2. **授权旋钮拧太快**。过早把决定权授给岗位 → 问责事故。防线:试用期零决定权是硬编码默认;每次扩权在岗位说明书留痕、主管签字。
3. **过程引擎复活**。信号:出现"过程状态推进"逻辑、Stella 内审批状态被当权威读。防线:经办人模型写进边界测试;"Stella 只记员工经手的事"。
4. **追人惹人烦**。催办变高级垃圾短信 → 被拉黑。防线:频率上限 + 升级规则;参与度指标盯负反馈。
5. **引擎沉没成本**。防线:Q2 闸门量化,失败不换场景重试,直接降级维护。
6. **组织记忆授权边界做错**。过宽即泄露,过窄即无用。防线:先窄后宽,消费方驱动扩权;分身隐私边界(归本人、管理员不可读)不可让步。
7. **单人产能 + 招人节奏**。防线:每幕一条主线;新人先接并行轨(部署/SSO/前端/副线);保活实验严格 timebox。
8. **网关单点赌注与迁移期裸 token**。Action Gateway 是唯一新承重构件,建错或建慢全盘受阻;迁移期沙箱仍持裸 token。防线:Q1 只建一类动作的窄网关,但该类必须**全链路封路**(凭证退出沙箱、直连路径封死)并先过对抗测试——窄而闭合,不宽而漏;外部写与跨人读优先入网关;裸 token 范围逐季度收缩,台账区分 gateway / legacy-direct / human-external 三种来源。

---

## 七、接下来 90 天

1. **Mock 周报**(Opus,一天):真实报销数据、真口径、可穿透样式 → 管理层反应 = 第一幕闸门;
2. 冻结计件契约 v0(纸面,两天,先于一切代码):含 outcome 唯一键、重开/撤销语义、归因规则、契约版本冻结,闸门阈值定成数字;
3. 最小雇佣记录 + 授权清单 v0 + Action Gateway v0(授权检查 + 回执一体)+ 报销 skill 接入;至少一类写动作**全链路封路**(凭证退出沙箱、只能经网关),越权对抗测试进 CI;
4. 对账巡检 + 验收证据快照上线;追人两类上线(未决审批、缺失材料),顺带解决驳回重提缺陷;
5. 周报上线:数字确定性生成、冻结快照,调度复用 Scheduler,李会计角色每周签收;
6. 参与度基线埋点开始记录;研发助理原型(`gh` CLI skill)给工程师用起来,同时记录人工 review 工时基线(Q2 引擎闸门的对照组);
7. K8s 外部 PG 冒烟;SSO 并行推进;HR 招聘岗位说明书纸面稿(为 Q2 准备);
8. 每月底对照 Q1 验收线检查,砍偏离主线的一切。
