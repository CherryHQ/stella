// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.29.0
// source: channel_ingress_cursor.sql

package sqlc

import (
	"context"
)

const advanceChannelIngressCursor = `-- name: AdvanceChannelIngressCursor :one
INSERT INTO channel_ingress_cursor (
    id, channel_id, platform, stream_key, cursor
)
VALUES (
    $1::uuid,
    $2,
    $3,
    $4,
    $5::bigint
)
ON CONFLICT (channel_id, platform, stream_key) DO UPDATE
SET cursor = GREATEST(channel_ingress_cursor.cursor, EXCLUDED.cursor),
    updated_at = now()
RETURNING id, channel_id, platform, stream_key, cursor, session_id, resume_gateway_url, session_state, last_error, created_at, updated_at
`

type AdvanceChannelIngressCursorParams struct {
	ID        string `json:"id"`
	ChannelID string `json:"channel_id"`
	Platform  string `json:"platform"`
	StreamKey string `json:"stream_key"`
	Cursor    int64  `json:"cursor"`
}

func (q *Queries) AdvanceChannelIngressCursor(ctx context.Context, arg AdvanceChannelIngressCursorParams) (ChannelIngressCursor, error) {
	row := q.db.QueryRow(ctx, advanceChannelIngressCursor,
		arg.ID,
		arg.ChannelID,
		arg.Platform,
		arg.StreamKey,
		arg.Cursor,
	)
	var i ChannelIngressCursor
	err := row.Scan(
		&i.ID,
		&i.ChannelID,
		&i.Platform,
		&i.StreamKey,
		&i.Cursor,
		&i.SessionID,
		&i.ResumeGatewayUrl,
		&i.SessionState,
		&i.LastError,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const ensureChannelIngressCursor = `-- name: EnsureChannelIngressCursor :one
INSERT INTO channel_ingress_cursor (
    id, channel_id, platform, stream_key, cursor
)
VALUES (
    $1::uuid,
    $2,
    $3,
    $4,
    $5::bigint
)
ON CONFLICT (channel_id, platform, stream_key) DO UPDATE
SET updated_at = now()
RETURNING id, channel_id, platform, stream_key, cursor, session_id, resume_gateway_url, session_state, last_error, created_at, updated_at
`

type EnsureChannelIngressCursorParams struct {
	ID        string `json:"id"`
	ChannelID string `json:"channel_id"`
	Platform  string `json:"platform"`
	StreamKey string `json:"stream_key"`
	Cursor    int64  `json:"cursor"`
}

func (q *Queries) EnsureChannelIngressCursor(ctx context.Context, arg EnsureChannelIngressCursorParams) (ChannelIngressCursor, error) {
	row := q.db.QueryRow(ctx, ensureChannelIngressCursor,
		arg.ID,
		arg.ChannelID,
		arg.Platform,
		arg.StreamKey,
		arg.Cursor,
	)
	var i ChannelIngressCursor
	err := row.Scan(
		&i.ID,
		&i.ChannelID,
		&i.Platform,
		&i.StreamKey,
		&i.Cursor,
		&i.SessionID,
		&i.ResumeGatewayUrl,
		&i.SessionState,
		&i.LastError,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const saveChannelIngressSessionState = `-- name: SaveChannelIngressSessionState :one
INSERT INTO channel_ingress_cursor (
    id, channel_id, platform, stream_key, cursor,
    session_id, resume_gateway_url, session_state, last_error
)
VALUES (
    $1::uuid,
    $2,
    $3,
    $4,
    $5::bigint,
    $6,
    $7,
    $8,
    $9
)
ON CONFLICT (channel_id, platform, stream_key) DO UPDATE
SET cursor = GREATEST(channel_ingress_cursor.cursor, EXCLUDED.cursor),
    session_id = EXCLUDED.session_id,
    resume_gateway_url = EXCLUDED.resume_gateway_url,
    session_state = EXCLUDED.session_state,
    last_error = EXCLUDED.last_error,
    updated_at = now()
RETURNING id, channel_id, platform, stream_key, cursor, session_id, resume_gateway_url, session_state, last_error, created_at, updated_at
`

type SaveChannelIngressSessionStateParams struct {
	ID               string `json:"id"`
	ChannelID        string `json:"channel_id"`
	Platform         string `json:"platform"`
	StreamKey        string `json:"stream_key"`
	Cursor           int64  `json:"cursor"`
	SessionID        string `json:"session_id"`
	ResumeGatewayUrl string `json:"resume_gateway_url"`
	SessionState     string `json:"session_state"`
	LastError        string `json:"last_error"`
}

func (q *Queries) SaveChannelIngressSessionState(ctx context.Context, arg SaveChannelIngressSessionStateParams) (ChannelIngressCursor, error) {
	row := q.db.QueryRow(ctx, saveChannelIngressSessionState,
		arg.ID,
		arg.ChannelID,
		arg.Platform,
		arg.StreamKey,
		arg.Cursor,
		arg.SessionID,
		arg.ResumeGatewayUrl,
		arg.SessionState,
		arg.LastError,
	)
	var i ChannelIngressCursor
	err := row.Scan(
		&i.ID,
		&i.ChannelID,
		&i.Platform,
		&i.StreamKey,
		&i.Cursor,
		&i.SessionID,
		&i.ResumeGatewayUrl,
		&i.SessionState,
		&i.LastError,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}
