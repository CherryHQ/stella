// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.29.0
// source: channel_group_command_receipt.sql

package sqlc

import (
	"context"
)

const createGroupCommandReceipt = `-- name: CreateGroupCommandReceipt :execrows
INSERT INTO channel_group_command_receipt (group_id, platform, message_id, command)
VALUES ($1, $2, $3, $4)
ON CONFLICT (group_id, platform, message_id) DO NOTHING
`

type CreateGroupCommandReceiptParams struct {
	GroupID   string `json:"group_id"`
	Platform  string `json:"platform"`
	MessageID string `json:"message_id"`
	Command   string `json:"command"`
}

// Claims the right to execute one inbound message's group command. Zero rows
// means the claim was already taken, i.e. this message is a redelivery and the
// command must not run again.
func (q *Queries) CreateGroupCommandReceipt(ctx context.Context, arg CreateGroupCommandReceiptParams) (int64, error) {
	result, err := q.db.Exec(ctx, createGroupCommandReceipt,
		arg.GroupID,
		arg.Platform,
		arg.MessageID,
		arg.Command,
	)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected(), nil
}

const deleteExpiredGroupCommandReceipt = `-- name: DeleteExpiredGroupCommandReceipt :exec
DELETE FROM channel_group_command_receipt
WHERE created_at < now() - interval '24 hours'
`

// Bounds the table without a background sweeper. Platform redeliveries land
// within minutes, so a day-old receipt can no longer dedup anything.
func (q *Queries) DeleteExpiredGroupCommandReceipt(ctx context.Context) error {
	_, err := q.db.Exec(ctx, deleteExpiredGroupCommandReceipt)
	return err
}

const deleteGroupCommandReceipt = `-- name: DeleteGroupCommandReceipt :exec
DELETE FROM channel_group_command_receipt
WHERE group_id = $1 AND platform = $2 AND message_id = $3
`

type DeleteGroupCommandReceiptParams struct {
	GroupID   string `json:"group_id"`
	Platform  string `json:"platform"`
	MessageID string `json:"message_id"`
}

// Releases a claim whose command did not run, so the next redelivery may retry.
func (q *Queries) DeleteGroupCommandReceipt(ctx context.Context, arg DeleteGroupCommandReceiptParams) error {
	_, err := q.db.Exec(ctx, deleteGroupCommandReceipt, arg.GroupID, arg.Platform, arg.MessageID)
	return err
}
