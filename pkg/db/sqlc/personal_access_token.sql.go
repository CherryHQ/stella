// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.29.0
// source: personal_access_token.sql

package sqlc

import (
	"context"

	"github.com/jackc/pgx/v5/pgtype"
)

const createPersonalAccessToken = `-- name: CreatePersonalAccessToken :one
INSERT INTO personal_access_token (
    public_id,
    user_id,
    name,
    token_hash,
    last4,
    scopes,
    expires_at,
    token_use
) VALUES ($1, $2, $3, $4, $5, $6, $7, $8)
RETURNING id, public_id, user_id, name, token_hash, last4, scopes, expires_at, last_used_at, revoked_at, created_at, updated_at, token_use
`

type CreatePersonalAccessTokenParams struct {
	PublicID  string             `json:"public_id"`
	UserID    string             `json:"user_id"`
	Name      string             `json:"name"`
	TokenHash string             `json:"token_hash"`
	Last4     string             `json:"last4"`
	Scopes    []string           `json:"scopes"`
	ExpiresAt pgtype.Timestamptz `json:"expires_at"`
	TokenUse  string             `json:"token_use"`
}

func (q *Queries) CreatePersonalAccessToken(ctx context.Context, arg CreatePersonalAccessTokenParams) (PersonalAccessToken, error) {
	row := q.db.QueryRow(ctx, createPersonalAccessToken,
		arg.PublicID,
		arg.UserID,
		arg.Name,
		arg.TokenHash,
		arg.Last4,
		arg.Scopes,
		arg.ExpiresAt,
		arg.TokenUse,
	)
	var i PersonalAccessToken
	err := row.Scan(
		&i.ID,
		&i.PublicID,
		&i.UserID,
		&i.Name,
		&i.TokenHash,
		&i.Last4,
		&i.Scopes,
		&i.ExpiresAt,
		&i.LastUsedAt,
		&i.RevokedAt,
		&i.CreatedAt,
		&i.UpdatedAt,
		&i.TokenUse,
	)
	return i, err
}

const getPersonalAccessTokenByPublicID = `-- name: GetPersonalAccessTokenByPublicID :one
SELECT id, public_id, user_id, name, token_hash, last4, scopes, expires_at, last_used_at, revoked_at, created_at, updated_at, token_use FROM personal_access_token
WHERE public_id = $1
`

// Lookup key is the indexed public_id. Revoked/expired filtering is done in the
// resolver so it can emit a precise error; the row is returned regardless of
// state here.
func (q *Queries) GetPersonalAccessTokenByPublicID(ctx context.Context, publicID string) (PersonalAccessToken, error) {
	row := q.db.QueryRow(ctx, getPersonalAccessTokenByPublicID, publicID)
	var i PersonalAccessToken
	err := row.Scan(
		&i.ID,
		&i.PublicID,
		&i.UserID,
		&i.Name,
		&i.TokenHash,
		&i.Last4,
		&i.Scopes,
		&i.ExpiresAt,
		&i.LastUsedAt,
		&i.RevokedAt,
		&i.CreatedAt,
		&i.UpdatedAt,
		&i.TokenUse,
	)
	return i, err
}

const listPersonalAccessTokenByUser = `-- name: ListPersonalAccessTokenByUser :many
SELECT id, public_id, user_id, name, token_hash, last4, scopes, expires_at, last_used_at, revoked_at, created_at, updated_at, token_use FROM personal_access_token
WHERE user_id = $1
  AND token_use = 'personal'
ORDER BY created_at DESC, id DESC
`

func (q *Queries) ListPersonalAccessTokenByUser(ctx context.Context, userID string) ([]PersonalAccessToken, error) {
	rows, err := q.db.Query(ctx, listPersonalAccessTokenByUser, userID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []PersonalAccessToken{}
	for rows.Next() {
		var i PersonalAccessToken
		if err := rows.Scan(
			&i.ID,
			&i.PublicID,
			&i.UserID,
			&i.Name,
			&i.TokenHash,
			&i.Last4,
			&i.Scopes,
			&i.ExpiresAt,
			&i.LastUsedAt,
			&i.RevokedAt,
			&i.CreatedAt,
			&i.UpdatedAt,
			&i.TokenUse,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const listProvisioningTokenByUser = `-- name: ListProvisioningTokenByUser :many
SELECT id, public_id, user_id, name, token_hash, last4, scopes, expires_at, last_used_at, revoked_at, created_at, updated_at, token_use FROM personal_access_token
WHERE user_id = $1
  AND token_use = 'provisioning'
ORDER BY created_at DESC, id DESC
`

func (q *Queries) ListProvisioningTokenByUser(ctx context.Context, userID string) ([]PersonalAccessToken, error) {
	rows, err := q.db.Query(ctx, listProvisioningTokenByUser, userID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []PersonalAccessToken{}
	for rows.Next() {
		var i PersonalAccessToken
		if err := rows.Scan(
			&i.ID,
			&i.PublicID,
			&i.UserID,
			&i.Name,
			&i.TokenHash,
			&i.Last4,
			&i.Scopes,
			&i.ExpiresAt,
			&i.LastUsedAt,
			&i.RevokedAt,
			&i.CreatedAt,
			&i.UpdatedAt,
			&i.TokenUse,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const revokePersonalAccessToken = `-- name: RevokePersonalAccessToken :execrows
UPDATE personal_access_token
SET revoked_at = now(),
    updated_at = now()
WHERE id = $1
  AND user_id = $2
  AND token_use = 'personal'
  AND revoked_at IS NULL
`

type RevokePersonalAccessTokenParams struct {
	ID     string `json:"id"`
	UserID string `json:"user_id"`
}

func (q *Queries) RevokePersonalAccessToken(ctx context.Context, arg RevokePersonalAccessTokenParams) (int64, error) {
	result, err := q.db.Exec(ctx, revokePersonalAccessToken, arg.ID, arg.UserID)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected(), nil
}

const revokePersonalAccessTokenByUser = `-- name: RevokePersonalAccessTokenByUser :execrows
UPDATE personal_access_token
SET revoked_at = now(),
    updated_at = now()
WHERE user_id = $1
  AND revoked_at IS NULL
`

// Cascade-revoke all of a user's PATs (e.g. on account deactivation).
func (q *Queries) RevokePersonalAccessTokenByUser(ctx context.Context, userID string) (int64, error) {
	result, err := q.db.Exec(ctx, revokePersonalAccessTokenByUser, userID)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected(), nil
}

const revokeProvisioningToken = `-- name: RevokeProvisioningToken :execrows
UPDATE personal_access_token
SET revoked_at = now(),
    updated_at = now()
WHERE id = $1
  AND user_id = $2
  AND token_use = 'provisioning'
  AND revoked_at IS NULL
`

type RevokeProvisioningTokenParams struct {
	ID     string `json:"id"`
	UserID string `json:"user_id"`
}

func (q *Queries) RevokeProvisioningToken(ctx context.Context, arg RevokeProvisioningTokenParams) (int64, error) {
	result, err := q.db.Exec(ctx, revokeProvisioningToken, arg.ID, arg.UserID)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected(), nil
}

const updatePersonalAccessTokenLastUsed = `-- name: UpdatePersonalAccessTokenLastUsed :execrows
UPDATE personal_access_token
SET last_used_at = now(),
    updated_at = now()
WHERE id = $1
  AND (
      last_used_at IS NULL
      OR last_used_at <= now() - interval '5 minutes'
  )
`

// Throttled to at most one write per 5 minutes, mirroring auth_user_token, so a
// hot token does not write the row on every API request.
func (q *Queries) UpdatePersonalAccessTokenLastUsed(ctx context.Context, id string) (int64, error) {
	result, err := q.db.Exec(ctx, updatePersonalAccessTokenLastUsed, id)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected(), nil
}
