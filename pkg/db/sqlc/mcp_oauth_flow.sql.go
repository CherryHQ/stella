// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.29.0
// source: mcp_oauth_flow.sql

package sqlc

import (
	"context"
	"encoding/json"
	"time"

	"github.com/jackc/pgx/v5/pgtype"
)

const consumeMCPOAuthFlow = `-- name: ConsumeMCPOAuthFlow :one
UPDATE mcp_oauth_flow
SET consumed_at = now()
WHERE id = $1 AND consumed_at IS NULL AND expires_at > now()
RETURNING id, server_id, user_id, credential_scope, credential_user_id, credential_agent_id, pkce_verifier, oauth_config, expires_at, consumed_at, created_at
`

// ConsumeMCPOAuthFlow flips consumed_at in the same statement that reads the
// row, so a replayed callback cannot ever complete the same flow twice.
func (q *Queries) ConsumeMCPOAuthFlow(ctx context.Context, id string) (McpOauthFlow, error) {
	row := q.db.QueryRow(ctx, consumeMCPOAuthFlow, id)
	var i McpOauthFlow
	err := row.Scan(
		&i.ID,
		&i.ServerID,
		&i.UserID,
		&i.CredentialScope,
		&i.CredentialUserID,
		&i.CredentialAgentID,
		&i.PkceVerifier,
		&i.OauthConfig,
		&i.ExpiresAt,
		&i.ConsumedAt,
		&i.CreatedAt,
	)
	return i, err
}

const createMCPOAuthFlow = `-- name: CreateMCPOAuthFlow :one
INSERT INTO mcp_oauth_flow (id, server_id, user_id, credential_scope, credential_user_id, credential_agent_id, pkce_verifier, oauth_config, expires_at)
VALUES ($1, $2, $3, $4, $5, $6, $7, $8, $9)
RETURNING id, server_id, user_id, credential_scope, credential_user_id, credential_agent_id, pkce_verifier, oauth_config, expires_at, consumed_at, created_at
`

type CreateMCPOAuthFlowParams struct {
	ID                string          `json:"id"`
	ServerID          string          `json:"server_id"`
	UserID            string          `json:"user_id"`
	CredentialScope   string          `json:"credential_scope"`
	CredentialUserID  pgtype.Text     `json:"credential_user_id"`
	CredentialAgentID pgtype.Text     `json:"credential_agent_id"`
	PkceVerifier      string          `json:"pkce_verifier"`
	OauthConfig       json.RawMessage `json:"oauth_config"`
	ExpiresAt         time.Time       `json:"expires_at"`
}

func (q *Queries) CreateMCPOAuthFlow(ctx context.Context, arg CreateMCPOAuthFlowParams) (McpOauthFlow, error) {
	row := q.db.QueryRow(ctx, createMCPOAuthFlow,
		arg.ID,
		arg.ServerID,
		arg.UserID,
		arg.CredentialScope,
		arg.CredentialUserID,
		arg.CredentialAgentID,
		arg.PkceVerifier,
		arg.OauthConfig,
		arg.ExpiresAt,
	)
	var i McpOauthFlow
	err := row.Scan(
		&i.ID,
		&i.ServerID,
		&i.UserID,
		&i.CredentialScope,
		&i.CredentialUserID,
		&i.CredentialAgentID,
		&i.PkceVerifier,
		&i.OauthConfig,
		&i.ExpiresAt,
		&i.ConsumedAt,
		&i.CreatedAt,
	)
	return i, err
}

const deleteExpiredMCPOAuthFlows = `-- name: DeleteExpiredMCPOAuthFlows :execrows
DELETE FROM mcp_oauth_flow WHERE expires_at < now()
`

func (q *Queries) DeleteExpiredMCPOAuthFlows(ctx context.Context) (int64, error) {
	result, err := q.db.Exec(ctx, deleteExpiredMCPOAuthFlows)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected(), nil
}

const getMCPOAuthFlow = `-- name: GetMCPOAuthFlow :one
SELECT id, server_id, user_id, credential_scope, credential_user_id, credential_agent_id, pkce_verifier, oauth_config, expires_at, consumed_at, created_at FROM mcp_oauth_flow WHERE id = $1
`

func (q *Queries) GetMCPOAuthFlow(ctx context.Context, id string) (McpOauthFlow, error) {
	row := q.db.QueryRow(ctx, getMCPOAuthFlow, id)
	var i McpOauthFlow
	err := row.Scan(
		&i.ID,
		&i.ServerID,
		&i.UserID,
		&i.CredentialScope,
		&i.CredentialUserID,
		&i.CredentialAgentID,
		&i.PkceVerifier,
		&i.OauthConfig,
		&i.ExpiresAt,
		&i.ConsumedAt,
		&i.CreatedAt,
	)
	return i, err
}
