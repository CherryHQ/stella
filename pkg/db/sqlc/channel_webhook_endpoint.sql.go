// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.29.0
// source: channel_webhook_endpoint.sql

package sqlc

import (
	"context"
	"time"

	"github.com/jackc/pgx/v5/pgtype"
)

const createChannelWebhookEndpoint = `-- name: CreateChannelWebhookEndpoint :one
INSERT INTO channel_webhook_endpoint (
    id,
    channel_id,
    owner_user_id,
    provider,
    token_public_id,
    token_hash,
    token_last4,
    provider_secret_ciphertext
) VALUES ($1, $2, $3, $4, $5, $6, $7, $8)
RETURNING id, channel_id, owner_user_id, provider, token_public_id, token_hash, token_last4, provider_secret_ciphertext, created_at, updated_at, rotated_at
`

type CreateChannelWebhookEndpointParams struct {
	ID                       string `json:"id"`
	ChannelID                string `json:"channel_id"`
	OwnerUserID              string `json:"owner_user_id"`
	Provider                 string `json:"provider"`
	TokenPublicID            string `json:"token_public_id"`
	TokenHash                string `json:"token_hash"`
	TokenLast4               string `json:"token_last4"`
	ProviderSecretCiphertext string `json:"provider_secret_ciphertext"`
}

func (q *Queries) CreateChannelWebhookEndpoint(ctx context.Context, arg CreateChannelWebhookEndpointParams) (ChannelWebhookEndpoint, error) {
	row := q.db.QueryRow(ctx, createChannelWebhookEndpoint,
		arg.ID,
		arg.ChannelID,
		arg.OwnerUserID,
		arg.Provider,
		arg.TokenPublicID,
		arg.TokenHash,
		arg.TokenLast4,
		arg.ProviderSecretCiphertext,
	)
	var i ChannelWebhookEndpoint
	err := row.Scan(
		&i.ID,
		&i.ChannelID,
		&i.OwnerUserID,
		&i.Provider,
		&i.TokenPublicID,
		&i.TokenHash,
		&i.TokenLast4,
		&i.ProviderSecretCiphertext,
		&i.CreatedAt,
		&i.UpdatedAt,
		&i.RotatedAt,
	)
	return i, err
}

const deleteChannelWebhookEndpoint = `-- name: DeleteChannelWebhookEndpoint :execrows
DELETE FROM channel_webhook_endpoint
WHERE id = $1
`

func (q *Queries) DeleteChannelWebhookEndpoint(ctx context.Context, id string) (int64, error) {
	result, err := q.db.Exec(ctx, deleteChannelWebhookEndpoint, id)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected(), nil
}

const getChannelWebhookEndpointByChannelID = `-- name: GetChannelWebhookEndpointByChannelID :one
SELECT id, channel_id, owner_user_id, provider, token_public_id, token_hash, token_last4, provider_secret_ciphertext, created_at, updated_at, rotated_at FROM channel_webhook_endpoint
WHERE channel_id = $1
`

func (q *Queries) GetChannelWebhookEndpointByChannelID(ctx context.Context, channelID string) (ChannelWebhookEndpoint, error) {
	row := q.db.QueryRow(ctx, getChannelWebhookEndpointByChannelID, channelID)
	var i ChannelWebhookEndpoint
	err := row.Scan(
		&i.ID,
		&i.ChannelID,
		&i.OwnerUserID,
		&i.Provider,
		&i.TokenPublicID,
		&i.TokenHash,
		&i.TokenLast4,
		&i.ProviderSecretCiphertext,
		&i.CreatedAt,
		&i.UpdatedAt,
		&i.RotatedAt,
	)
	return i, err
}

const getChannelWebhookEndpointByID = `-- name: GetChannelWebhookEndpointByID :one
SELECT id, channel_id, owner_user_id, provider, token_public_id, token_hash, token_last4, provider_secret_ciphertext, created_at, updated_at, rotated_at FROM channel_webhook_endpoint
WHERE id = $1
`

func (q *Queries) GetChannelWebhookEndpointByID(ctx context.Context, id string) (ChannelWebhookEndpoint, error) {
	row := q.db.QueryRow(ctx, getChannelWebhookEndpointByID, id)
	var i ChannelWebhookEndpoint
	err := row.Scan(
		&i.ID,
		&i.ChannelID,
		&i.OwnerUserID,
		&i.Provider,
		&i.TokenPublicID,
		&i.TokenHash,
		&i.TokenLast4,
		&i.ProviderSecretCiphertext,
		&i.CreatedAt,
		&i.UpdatedAt,
		&i.RotatedAt,
	)
	return i, err
}

const getWebhookChannelBindingForUpdate = `-- name: GetWebhookChannelBindingForUpdate :one
SELECT
    channel.id,
    channel.type,
    channel.agent_id,
    COALESCE(agent.enabled, false) AS agent_enabled,
    channel.config
FROM channel
LEFT JOIN agent ON agent.id = channel.agent_id
WHERE channel.id = $1
FOR UPDATE OF channel
`

type GetWebhookChannelBindingForUpdateRow struct {
	ID           string      `json:"id"`
	Type         string      `json:"type"`
	AgentID      pgtype.Text `json:"agent_id"`
	AgentEnabled bool        `json:"agent_enabled"`
	Config       string      `json:"config"`
}

func (q *Queries) GetWebhookChannelBindingForUpdate(ctx context.Context, id string) (GetWebhookChannelBindingForUpdateRow, error) {
	row := q.db.QueryRow(ctx, getWebhookChannelBindingForUpdate, id)
	var i GetWebhookChannelBindingForUpdateRow
	err := row.Scan(
		&i.ID,
		&i.Type,
		&i.AgentID,
		&i.AgentEnabled,
		&i.Config,
	)
	return i, err
}

const resolveChannelWebhookEndpointByPublicID = `-- name: ResolveChannelWebhookEndpointByPublicID :one
SELECT
    endpoint.id AS endpoint_id,
    endpoint.channel_id,
    endpoint.owner_user_id,
    endpoint.provider,
    endpoint.token_hash,
    endpoint.token_last4,
    endpoint.provider_secret_ciphertext,
    endpoint.created_at,
    endpoint.updated_at,
    endpoint.rotated_at,
    channel.agent_id,
    channel.enabled AS channel_enabled,
    auth_user.is_active AS owner_active,
    agent.enabled AS agent_enabled
FROM channel_webhook_endpoint AS endpoint
JOIN channel ON channel.id = endpoint.channel_id
JOIN auth_user ON auth_user.id = endpoint.owner_user_id
JOIN agent ON agent.id = channel.agent_id
WHERE endpoint.token_public_id = $1
  AND channel.type = 'webhook'
  AND channel.enabled = true
  AND auth_user.is_active = true
  AND agent.enabled = true
`

type ResolveChannelWebhookEndpointByPublicIDRow struct {
	EndpointID               string             `json:"endpoint_id"`
	ChannelID                string             `json:"channel_id"`
	OwnerUserID              string             `json:"owner_user_id"`
	Provider                 string             `json:"provider"`
	TokenHash                string             `json:"token_hash"`
	TokenLast4               string             `json:"token_last4"`
	ProviderSecretCiphertext string             `json:"provider_secret_ciphertext"`
	CreatedAt                time.Time          `json:"created_at"`
	UpdatedAt                time.Time          `json:"updated_at"`
	RotatedAt                pgtype.Timestamptz `json:"rotated_at"`
	AgentID                  pgtype.Text        `json:"agent_id"`
	ChannelEnabled           bool               `json:"channel_enabled"`
	OwnerActive              bool               `json:"owner_active"`
	AgentEnabled             bool               `json:"agent_enabled"`
}

func (q *Queries) ResolveChannelWebhookEndpointByPublicID(ctx context.Context, tokenPublicID string) (ResolveChannelWebhookEndpointByPublicIDRow, error) {
	row := q.db.QueryRow(ctx, resolveChannelWebhookEndpointByPublicID, tokenPublicID)
	var i ResolveChannelWebhookEndpointByPublicIDRow
	err := row.Scan(
		&i.EndpointID,
		&i.ChannelID,
		&i.OwnerUserID,
		&i.Provider,
		&i.TokenHash,
		&i.TokenLast4,
		&i.ProviderSecretCiphertext,
		&i.CreatedAt,
		&i.UpdatedAt,
		&i.RotatedAt,
		&i.AgentID,
		&i.ChannelEnabled,
		&i.OwnerActive,
		&i.AgentEnabled,
	)
	return i, err
}

const rotateChannelWebhookEndpoint = `-- name: RotateChannelWebhookEndpoint :one
UPDATE channel_webhook_endpoint
SET token_public_id = $1,
    token_hash = $2,
    token_last4 = $3,
    provider_secret_ciphertext = $4,
    rotated_at = now(),
    updated_at = now()
WHERE id = $5
RETURNING id, channel_id, owner_user_id, provider, token_public_id, token_hash, token_last4, provider_secret_ciphertext, created_at, updated_at, rotated_at
`

type RotateChannelWebhookEndpointParams struct {
	TokenPublicID            string `json:"token_public_id"`
	TokenHash                string `json:"token_hash"`
	TokenLast4               string `json:"token_last4"`
	ProviderSecretCiphertext string `json:"provider_secret_ciphertext"`
	ID                       string `json:"id"`
}

func (q *Queries) RotateChannelWebhookEndpoint(ctx context.Context, arg RotateChannelWebhookEndpointParams) (ChannelWebhookEndpoint, error) {
	row := q.db.QueryRow(ctx, rotateChannelWebhookEndpoint,
		arg.TokenPublicID,
		arg.TokenHash,
		arg.TokenLast4,
		arg.ProviderSecretCiphertext,
		arg.ID,
	)
	var i ChannelWebhookEndpoint
	err := row.Scan(
		&i.ID,
		&i.ChannelID,
		&i.OwnerUserID,
		&i.Provider,
		&i.TokenPublicID,
		&i.TokenHash,
		&i.TokenLast4,
		&i.ProviderSecretCiphertext,
		&i.CreatedAt,
		&i.UpdatedAt,
		&i.RotatedAt,
	)
	return i, err
}
