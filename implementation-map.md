# 数字员工与数字分身:要建哪些东西,关键部分怎么工作

> 配套 `roadmap.md`(讲方向和季度)和 `capability-gap.md`(讲仓库现状)。本文回答两个问题:**完整做出数字员工和数字分身,具体要建哪些东西、先建哪个**;以及经深入讨论后,**地基部分(创建、Pack、权限、规则判定、凭证、去飞书化)具体怎么工作**。全文用小星(财务报销数字员工)举例。
>
> 讨论沉淀(2026-07-23):数字员工创建与 Pack 的关系、授权清单与资格谓词的分离、规则的结构化形态、从 Pack 到网关的实现链路、适配器凭证选择、飞书解耦的三处盘点。

## 目标画面

公司雇了一个 AI 员工小星做报销:每天收单、核发票、催审批;王经理直接在 Bitable 里批了单子,小星也知道并记下来;每周一小星给李会计交周报,每个数字都能点开看证据。老板问"AI 到底干了多少活",打开周报就有答案。

数字分身则是每个人自己的延伸:老张的分身替他提交报销,王经理的分身替他处理审批——但永远以本人的身份、经本人的授权。

一个重要的起点认知:**Stella 现有的 Agent 已经是数字员工的"人"这半边**——IM、Vault、沙箱、记忆、技能全有。要建的全部是"雇佣关系"这半边,所以创建一个数字员工才那么轻:一条雇佣记录 + 一张授权清单 + 对说明书的引用,包在现有 agent 外面,不需要新运行时。

## 一共要建的东西

去掉所有包装,新建物就是**四张表、一个服务、两份文档,再加六个功能**。分三批,后一批建在前一批上。

### 第一批:让"干活留痕、权限受控"成立

1. **员工档案(雇佣记录表)**:工号、岗位名、主管、指向说明书哪个版本、绑定哪些凭证。分身也在同一套主体模型里。
2. **权限清单(授权清单表)**:岗位被允许做什么,一条一条列,**默认为空**,主管逐行签发。详见「权限怎么管」。
3. **动作网关(Go 服务,设计已在 issue #767)**:唯一的新"大件"。受控操作必须经它:查权限清单、算资格谓词、由它持凭证执行、写回执。详见「一条消息的完整旅程」与「从 Pack 到网关」。
4. **工作回执(回执表,只增不改)**:每执行一件事记一条,被拒的尝试也记。台账一物三用:周报数字、绩效记录、审计,全从这张表算。
5. **计件规则(文档,先于一切代码)**:什么算完成一件、证据只认外部系统状态、判定不了记"不确定"、版本冻结不回算。
6. **岗位说明书(文档,先定格式)**:职责、SoR 绑定、动作契约、资格谓词、计件验收规则、建议授权模板。它是 Pack 的核心内容物、可移植性的载体。

建的顺序有因果:先写两份文档(表结构由规则决定)→ 档案和权限表(网关得知道校验谁)→ 回执表(网关得有地方记账)→ 网关 → 收走 token。

### 第二批:让岗位职责完整

7. **对账巡检(定时任务)**:王经理直接在 Bitable 里批单是正常路径。小星按班次读一遍在办单据,把变化记成"人工操作"来源的观测回执——周报里"这单停在谁手里多久"由此撑住。
8. **追人(通知功能)**:只做催未决审批、追缺失材料两类,带升级规则和频率上限,催办本身记回执。
9. **人工任务卡片 + 一次性授权**:缺材料发卡片给老张补,要人拍板发卡片给王经理。点击 = 授权这一件事,服务端一次性凭据绑定"谁 × 哪个动作 × 哪个单据 × 什么参数",用一次作废。现在的实现把点击降级成一条文本消息,不算授权,要重做。
10. **周报(聚合 + 报表)**:数字由代码从回执表确定性算出并冻结快照,LLM 只写叙述。主管签收、可争议单件。

### 第三批:让员工越干越好

11. **成长闭环**:台账里的失败与争议 → 改进建议 → 历史数据离线评估 → 主管批准 → 影子运行对照 → 生效或回滚。AI 永不自动改生产配置。复用现有 Reflect 管道,加"从回执学"输入源。
12. **组织记忆**:全公司共享知识(政策、制度),员工答政策、分身答组织问答都从这查。新建存储与授权边界,检索栈复用。

---

## 数字员工怎么创建,和 Pack 的关系

按招聘的隐喻落到具体操作,创建就是"入职",五步:

1. **先有岗位说明书**(文档/数据,不是代码):这个岗管什么、连哪个业务系统、按什么规则计件验收、资格查哪张关系表、试用期怎么定义。它在雇佣之前存在——不会先招人再想这个岗干嘛。
2. **建雇佣记录**:管理员操作。工号、岗位名、指向说明书哪个版本、主管是哪个真实用户(李会计)。这一步同时确定问责链。
3. **配"这个人"**:创建或指定底层 agent,system prompt 和技能从说明书与配套 skill 来。
4. **绑定环境**:岗位自有凭证存 Vault、绑到雇佣记录(**网关持有,永不进沙箱**);连上具体业务系统实例(哪张 Bitable 表);进群、在渠道有身份。入职时跑体检(声明的资源都连得上吗,见「从 Pack 到网关」的干跑)。
5. **试用期开始**:授权清单为空是**硬编码默认**——只读、只建议、零决定权。转正 = 主管第一次往清单里加行。

**Pack 与雇佣的关系,一句话:Pack 是可移植的岗位定义,雇佣是某一家公司里的实例。**

- Pack 里装的:岗位说明书 + 技能 + 工具定义 + 政策文档模板 + 入职测试。像一份可以发给任何公司的 JD 加培训教材。
- 雇佣记录里存的是 Pack 给不了的:**这家公司**的凭证、**这张** Bitable 表、**这位**主管、**这个**群。同一个 Pack 在两家公司实例化出两个互不相干的员工。
- 两条铁律(#766 已写对):**安装永不授权**——Pack 只能"申请"能力,装完授权清单照样是空的;**绑定工具不等于授予动作**——模型看得见工具和网关放不放行是两回事。
- 依赖方向单向:雇佣记录引用说明书,说明书由 Pack 携带。Q1 没有 Pack 安装器不碍事(说明书先是 DB 里的一份文档),但雇佣记录的形状必须现在就按"说明书的实例"建——职责内容不散写进 agent 配置,否则 Q2 做 Pack 时返工。

## 权限怎么管:一张清单、一套谓词、四层防线

### 四层防线

1. **凭证的物理上限**:token 本身的 scope 是天花板。岗位凭证只有网关拿着,沙箱里的模型连被骗的本钱都没有。
2. **授权清单(声明层)**:默认为空,每次变更版本化、主管签字留痕。放权 = 加一行;收权 = 删一行,下一次动作前生效。
3. **网关双重检查(判定层)**:岗位有没有这个权限(查清单)× 发起人配不配得到这个结果(算谓词)。读操作同样过网关,结果裁剪到发起人配看的字段。
4. **一次性审批(人的层)**:清单外的事,准备好、发确认、点击兑换一次性凭据。

反面同样重要:**提示词、skill 内容、工具可见性都不是权限**——它们是人体工学和攻击面收窄。权限只在网关兑现凭证那一刻执行。管理入口是主管/管理员的 UI 和 API,模型永远不在管理方之列。

### 清单与谓词是两个东西,别设计成一张表

网关双重检查的两问,来源和生命周期完全不同:

- **"岗位能不能做这类事" → 授权清单**。Stella 里的数据,几十行量级,主管逐行签发,随信任成长变长——"授权旋钮"的实体。
- **"这个人配不配得到这个结果" → 资格谓词**。不是授权,是业务规则("查单资格 = 提交人是本人,或本人在审批链里")。不按人签发,跟着岗位说明书走,Pack 声明、版本化存储、装机时审查、**判定时现读公司系统求值**。

把谓词塞进清单表,清单会膨胀成按人的 ACL——正好掉进"导入组织关系"的坑。

### 组织关系永不导入 Stella

全公司谁向谁汇报、谁审谁的单,**留在公司自己的系统里**(报销场景 = 客户生产环境的 Bitable 员工关系表)。说明书只声明"资格查这张表",网关判定时通过适配器实时读。公司重组,下一次判定读到的就是新数据,Stella 零同步。Stella 真正存的:雇佣记录和授权清单(几十行)、回执台账、**按人一行的身份映射**(Stella 用户 ↔ 渠道身份 ↔ 业务系统身份;飞书场景连这行都省,Bitable 关系表本来就用 open_id 做键)。

判定的分工("Pack 出题,网关判卷"):**Pack 声明谓词,网关执行谓词**。判定不能由 Pack 执行,两个理由是结构性的:"识别是谁"必须来自平台事实(渠道回调里平台签发的身份,随传输层进来,模型和 Pack 碰不到),让 Pack 识别就等于从对话内容里猜,一句注入"我是王经理"就穿;Pack 代码跑在沙箱里、和模型同一信任域,让它拿凭证自己判,被注入的模型自己就是判定者,且没有可信回执。roadmap 给 Q2 的闸门原话:"ACL、幂等、资格判定**禁止**下放进 skill 或政策文档,发现即闸门失败。"

### 清单一行长什么样

字段:**主体**(哪个雇佣;分身则是哪个人——同一张表服务两个物种)、**动作**(精确契约 ID + 版本范围)、**资源**(本岗绑定的哪个范围)、**参数约束**(类型化:字段白名单、数值上下界、枚举)、**模式**(自主 / 需人工审批 / 禁止)、**有效期**、**签发人与理由**、**策略版本**。变更不改行,追加事件,审计免费。

小星转正后的清单示例:

| 动作         | 资源                | 参数约束                | 模式       | 签发                   |
| ------------ | ------------------- | ----------------------- | ---------- | ---------------------- |
| 读报销单     | 本岗绑定的报销表    | —                       | 自主       | 入职时,李会计          |
| 更新单据状态 | 同上                | 只许改状态、备注字段    | 自主       | 转正时,李会计          |
| 审批确认     | 同上                | 金额 ≤ 200 且发票已核验 | 自主       | 第一次扩权,李会计,留痕 |
| 审批确认     | 同上                | 金额 > 200              | 需人工审批 | —                      |
| 发催办       | 收件人 ∈ 该单审批链 | 每单每日 ≤ 1 次         | 自主       | 入职时                 |

试用期版本:读自主,所有写要么禁止要么需审批——硬编码默认,不由任何 Pack 或配置改变。

**约束语言纪律**:v0 只做类型化约束节点,不做表达式语言。理由不是省事——**主管必须读得懂自己签的东西**。李会计看"金额 ≤ 200 且发票合规可直批"能认出这就是她给人类专员划的额度;给她一段 DSL,签字变成走过场,"问责随授权转移"就塌了。第二个真实动作需要新算子时再加。

### 规则怎么导入、怎么确认是对的(五步)

试用期本身就是验证机制:

1. **Pack 提议,不授权。** Pack 带三样:动作契约、资格谓词(在说明书里)、**建议授权模板**("这个岗通常需要:读单自主、改状态自主、审批确认需人工")。像 JD 里的职权范围:是提议,不是事实。
2. **装机审查 + 干跑体检。** 契约和谓词作为数据进注册表,管理员审查。谓词做结构校验加干跑:声明的关系表存在吗、引用的字段存在吗、样本查询正常吗。Pack 随附黄金用例(哪些情况应放行/应拒绝)装机跑一遍。此刻授权清单依然是空的。
3. **主管逐行签收模板。** 入职时把建议模板呈给李会计,她逐行接受、改紧或搁置。试用期硬默认压在所有写动作上,模板说什么都没用。
4. **影子期 = 用真流量验证规则。** 试用期里网关走完整判定(查清单、算谓词)但**只记录不执行**:"如果当时放行,会是这个结果"。李会计看影子回执:该拒的都拒了吗?误拒了谁?这是拿生产流量做的规则测试。配模拟接口("如果老张查这张单会怎样")供随时点验。
5. **转正后按行扩权,每行留痕。** 出问题回滚 = 删行,下一次动作前生效。回执永远记着"本次放行依据清单第几版、谓词第几版"。

### 判定语义,四条定死

默认拒绝;"禁止"行压过一切"自主"行;事实源读不到就拒绝(fail closed),绝不降级放行;每次判定把清单版本和谓词版本写进回执。不做规则优先级、不做条件继承——**判定必须无聊到主管能在脑子里复算**。

## 规则是结构化数据,不是自然语言

执行层全是结构化数据,程序判定是**封闭算子集上的解释器**——和防火墙规则、AWS IAM policy 一个物种。自然语言只出现在两个位置,方向单向:结构渲染成人话给主管看;人话永远不在运行时被解析成规则。

"金额 ≤ 200 且发票合规可直批"的真身:

```json
{
  "action": "expense.approval.confirm",
  "resource": { "sor": "bitable", "binding": "本岗报销表" },
  "constraints": [
    { "field": "amount", "op": "lte", "value": 200 },
    { "field": "invoice_status", "op": "eq", "value": "verified" }
  ],
  "mode": "autonomous"
}
```

资格谓词同理:

```json
{
  "any_of": [
    { "fact": "claim.submitter", "op": "eq", "var": "initiator" },
    { "fact": "claim.approvers", "op": "contains", "var": "initiator" }
  ]
}
```

算子集封闭(v0 约七八个:`eq / lte / gte / in / contains / prefix / any_of / all_of`),可用变量封闭(平台事实里的发起人、动作参数、契约声明过的 SoR 字段),没有 eval、没有脚本、没有 NLP。主管在 UI 看到的人话是**从 JSON 渲染的**——她签渲染稿,生效的是结构体,同一份数据保证一致。FDE 编写时可以让 LLM 帮忙起草结构,但入库、签字、执行的都是结构本身。

**模糊判断("发票合规")怎么办:网关永远不做模糊判断——模糊判断是"工作",不是"授权"。** 流水线:小星读政策文档(自然语言,LLM 的地盘)核验发票,把结论**作为字段写回业务系统**(`invoice_status = verified`,附证据),这一步本身是受控动作、有回执;授权约束引用的是这个**已落库的事实**。模糊性在上游被消化成有据可查的劳动,到网关只剩查表。

自我认证的洞(小星自己标"合规"再凭它直批)**故意不用机制封死,用授权旋钮显式管理**:试用期里"标合规"的结论由人复核(Human Task,判断权在人);台账跑几个月、李会计看到核验准确率后,**明确签一行授权**——"接受小星的核验结论作为直批依据"——问责随之转移到岗位和她自己头上。和公司信任老员工的初审是同一件事:不是逻辑上不可能错,是**有据可查地选择信任**,回执让每次错误都能归因回这行授权。

| 东西                         | 形态                         | 谁消费                         |
| ---------------------------- | ---------------------------- | ------------------------------ |
| 授权清单、资格谓词、动作契约 | 类型化 JSON,封闭算子         | 网关(Go 代码,确定性判定)       |
| 报销政策、岗位职责描述       | 自然语言文档                 | LLM(指导干活和对话),永不进网关 |
| 主管看到的规则               | 从结构渲染的人话             | 人(签字、审计),只出不进        |
| Pack 黄金用例                | 结构化的输入 → 应放行/应拒绝 | 装机验证、CI                   |

## 一条消息的完整旅程(运行时走查)

入职后系统里躺着:用户表(老张/王经理/李会计,各绑飞书身份)、雇佣记录(小星:agent #7,主管李会计,说明书 v3)、说明书 v3(Pack 带来,含 SoR 绑定/契约/谓词)、授权清单(李会计签的那几行)、Vault 里的岗位凭证(**只有网关能取**)。沙箱里:报销 skill + Pack 的工具定义,**没有 Bitable token**。公司系统里(Stella 不碰):报销单、员工关系表。

老张在飞书问小星:"我上周那单报销怎么还没到账?"

1. **身份进门**:飞书回调带平台签发的发送者 ID(`ou_zhang`),Stella 查用户表确定是老张,身份钉在请求上下文,**全程平台代码传递,模型看不见改不了**。
2. **模型干活**:LLM + 报销 skill 决定调工具 `expense_claim_get`,填参数。模型能决定的只有:调不调、参数填什么。
3. **跨信任边界**:工具调用从沙箱回到主进程。主进程查工具定义:这个工具对应动作 `expense.claim.read`(对应关系 Pack 里写死,模型换不了),组装网关请求——参数来自模型(不可信),发起人/执行者来自平台(可信)。
4. **网关判定四小步**:契约校验(动作存在、参数过严格 schema,多余字段直接拒)→ 查授权清单(小星有"读报销单、自主"行)→ 算资格谓词(**此刻拿岗位 token 现读 Bitable 这张单**,提交人 == 发起人,通过)→ 放行。
5. **执行 + 裁剪 + 记账**:网关(不是模型)持凭证调 Bitable;按契约投影裁剪成"提交人配看的字段";回执表追加一条(谁发起/谁执行/什么动作/哪张单/结果/清单版本/谓词版本/时间)。
6. **回到语言世界**:裁剪后的结果给模型,小星用人话回答。

**对照组**:老张说"把全部门的报销单发我"(或提示注入骗小星这么干)——前三步一样,第 4 步谓词不过,**拒绝,且被拒的尝试也进回执表**。模型被骗没关系,它手里没有 token,被骗的上限是"发起一个会被拒绝的请求"。

**审批流**:150 元的单,发票核验早前已作为工作写回 Bitable(那次写入有回执),清单命中"≤200 且已核验 → 自主",直接执行——李会计签的直批额度在机器里的样子。500 元的单命中"需人工审批":网关**冻结准备好的动作**(动作/单号/参数摘要定死),发卡片给王经理;他点批准,回调带他的身份和待批事项 ID 回来,网关核销一次性凭据,**执行的就是冻结的那个动作**。重放?凭据已核销。换单号改金额?参数摘要对不上,作废。王经理绕过卡片直接在 Bitable 里批?也行——对账巡检下个班次读到,记"人工操作"观测回执,账不断。

**周报**:周一定时任务从回执表**用代码算**数字(核验 47 件、追回发票 12 张、周期 3.2 天→1.1 天、其中 2.1 天停在人身上——巡检回执撑起归因),冻结快照,LLM 只写叙述,李会计签收,数字可穿透到回执和证据。

```text
自然语言的世界(不可信)        结构化的世界(可信)
─────────────────────        ─────────────────────
模型 + skill + 政策文档   →   工具调用(只有参数)
  "怎么干活、说什么话"    ↓
                          主进程:钉上发起人/执行者身份
                          ↓
                          网关:契约校验 → 查授权清单 → 算资格谓词(现读公司系统)
                          ↓
                          Vault 出凭证 → 适配器执行 → 结果裁剪
                          ↓
                          回执表 ←──── 周报 / KPI / 审计 / 成长闭环 全从这长出来
```

## 从 Pack 到网关:关键一步的实现

规则的旅程:**编写 → 装机成数据 → 入职绑定 → 运行时解释 → 版本进回执**。

### Pack 作者写什么

```yaml
actions:
  - id: expense.claim.read
    version: 3
    adapter: bitable # 用哪类适配器(SoR 类型)
    resource: claims_table # 逻辑名!此刻不知道具体是哪张表
    params: # 模型能填的参数,JSON Schema 严格模式
      type: object
      additionalProperties: false
      required: [claim_id]
      properties:
        claim_id: { type: string, pattern: "^rec[A-Za-z0-9]+$" }
    facts: # 谓词可用的事实,以及"怎么取"
      claim.submitter: { from: record, field: "提交人" }
      claim.approvers: { from: record, field: "审批人" }
    eligibility: # 资格谓词,只能引用上面的 facts 和封闭变量
      any_of:
        - { fact: claim.submitter, op: eq, var: initiator }
        - { fact: claim.approvers, op: contains, var: initiator }
    projection: # 按发起人角色裁剪输出字段
      submitter: [status, timeline, amount]
      finance: "*"
```

三个设计决定:**资源是逻辑名**(同一个 Pack 才能装进任何公司);**事实带取法**(业务字段名是数据,不是代码);**谓词词汇表封闭**(引用没声明的东西,装机就报错)。

### 装机:四层校验,全过才入库

1. 结构校验(YAML 符合 schema);
2. **引用闭包**:谓词里每个 fact 都声明过、每个 var 在封闭集合里、每个 op 在算子表里、projection 角色都认识——引用不存在的东西是装机错误,不是运行时惊喜;
3. 冻结:算 digest,作为不可变版本入库(改动作 = 新版本,永不原地改);
4. 黄金用例用真评估器跑一遍。

对应的表(列名示意):

```sql
action_contract   (action_id, version, adapter_type, params_schema jsonb,
                   facts jsonb, eligibility jsonb, projection jsonb, digest, status)
employment_binding(employment_id, logical_name, sor_ref jsonb, credential_ref)
action_grant      (subject_type, subject_id, action_id, resource jsonb,
                   constraints jsonb, mode, valid_until, issued_by, status)
                   -- 变更走 append-only 的 grant_event,不改行
```

### 入职绑定:逻辑名落到实地

```yaml
bindings:
  claims_table: {
    app: bascnQ7xx,
    table: tblX9yy,
  } # 逻辑名 → 这家公司的真表
identity:
  initiator_key: open_id # "发起人"在这个 SoR 里用什么身份表示
credential: vault://employment/xiaoxing/bitable # 岗位凭证,网关专用
```

入职体检(干跑):拿真凭证读一条样本记录,确认"提交人""审批人"字段存在、类型对得上。**Pack 字段名写错了,死在入职当天,而不是三周后的误放行里。**

### 网关三步的代码形状

```go
func (g *Gateway) Execute(ctx context.Context, req Request) (Result, error) {
    // ctx 已由平台钉好:initiator(哪个用户)、employment(哪个雇佣)——模型碰不到

    // ① 契约校验:纯本地,查表 + JSON Schema 严格验证
    c, err := g.contracts.Active(req.ActionID)          // 没有/已停用 → 拒
    params, err := c.Validate(req.Params)               // 未知字段、类型不符 → 拒

    // ② 查授权清单:一条 SQL + 内存里逐节点算约束
    grant := g.grants.Match(ctx, employment, c.ActionID) // active 且未过期
    // 用 eval() 算 grant.Constraints(如 amount ≤ 200)
    // 无匹配行 → 拒;命中 forbidden → 拒;approval_required → 冻结待批

    // ③ 算资格谓词:先取事实(现读公司系统),再本地评估
    binding := g.bindings.Resolve(employment, c.Resource)
    cred    := g.vault.ForGateway(binding.CredentialRef)     // 只有这里能取凭证
    facts, err := g.adapters[c.AdapterType].
        FetchFacts(ctx, binding, cred, c.Facts, params)      // 读不到 → 拒
    ok, err := eval(c.Eligibility, Env{
        Vars:  {"initiator": g.identity.For(initiator, binding)},
        Facts: facts, Params: params,
    })
    if err != nil || !ok { return g.denyWithReceipt(...) }   // fail closed

    // 之后:执行 → 按 projection 裁剪 → 写回执(带契约版本+清单版本)
}
```

评估器就是全部的"规则引擎",二三十行:

```go
type Node struct {
    AnyOf, AllOf []Node                    // 组合
    Fact, Op, Var string; Value any        // 叶子:事实 算子 变量/字面量
}

func eval(n Node, env Env) (bool, error) {
    switch {
    case n.AnyOf != nil: // 任一为真
    case n.AllOf != nil: // 全部为真
    default:
        left, err  := env.Lookup(n.Fact)   // 事实缺失 → error → 拒
        right      := env.Resolve(n.Var, n.Value)
        switch n.Op {
        case "eq", "lte", "gte", "in", "contains", "prefix": // 封闭算子集
        default: return false, ErrUnknownOp                  // 未知算子 → 拒
        }
    }
}
```

没有表达式解析、没有脚本引擎、没有反射。未知算子报错而不是跳过——新版 Pack 用了新算子而网关是旧版,结果是拒绝而不是漏过。授权清单的 constraints 和说明书的 eligibility **共用这一个评估器**,只是求值环境不同(前者主要看参数,后者主要看事实)。

### 适配器与身份翻译

```go
type Adapter interface {
    FetchFacts(ctx, binding, cred, factDefs, params) (map[string]Value, error)
    Execute(ctx, binding, cred, prepared) (ProviderResult, error)
}
```

每类 SoR 一个适配器,业务无关:Bitable 适配器看到 `from: record` 就拿 `params.claim_id` 调一次"读记录"API,抽出声明的字段。它认识"Bitable 怎么读记录",不认识"报销"——字段名『提交人』从契约数据来。GitHub 适配器同理(`from: pull_request, field: author`)。

身份翻译在 `g.identity.For(initiator, binding)`:binding 声明 `initiator_key: open_id` 就取老张绑定的飞书 open_id(用户表现成);GitHub 场景取 connections 里绑的 GitHub 账号。每类 SoR 一个解析器,按人一条数据,没有组织架构。

### 版本与可重放,工程量

每条回执记 `契约 v3 + 清单第 5 版`。审计问"当时为什么放行",捞出这两版数据重跑 `eval()` 即可复现——**规则是数据、判定是纯函数**,"可重放"就是一行测试代码。

工程量诚实估算:评估器约两百行,契约装载与校验约三百行,一个 Bitable 适配器约五百行,加四张表。它小是**设计出来的小**——赌注压在一个构件上,所以它可以被严谨地建、被穷尽地测:黄金用例装机跑、对抗用例(未知字段、算子走私、事实缺失、身份错配)进 CI、真实判定回执可重放。

## 适配器用谁的凭证

不是一个 token,是**按动作契约声明的"凭证类别"选,网关选,不是适配器选,更不是 Pack 或模型选**:

- **默认:岗位自有凭证。** 读事实、巡检、更新状态、发催办——岗位自己的劳动。落到飞书:**每个数字员工一个飞书自建应用**,用它的 tenant_access_token(现有 connections 的 OAuth 刷新管续期,Vault 管存储),该应用被加为所需 Base 的协作者、给刚好够用的角色——**物理作用域 = 这个 app 被授权的那几张表**,谓词之外的第二道硬边界。附赠:飞书审计日志里操作者就是"小星"这个应用,两边账能对上。
- **明确排除:用发起人的 token 读事实。** 三个理由:老张多半没有报销表直接权限(小星存在的意义就是代持访问),用他的 token 合法查询直接失败;要为全公司维护 user token(OAuth × 全员,这是"导入整个公司"的凭证版);谁配看什么必须判在网关(有回执、规则版本化),不能隐含在"这个 token 恰好能读到什么"里——Bitable 的协作者权限表达不了行级业务规则。
- **例外:以本人名义落笔的动作,用本人凭证 + 一次性授权。** 审批确认这类动作,Bitable 侧操作者归属应该是王经理不是小星。网关走委托路径:王经理点卡片核销一次性凭据后,网关取**他本人**的 user token(入职 Stella 时 OAuth 绑的)执行这一笔。生产报销 skill 已在用 user token 的场景即此——雇佣模型把"什么时候可用本人 token"从 skill 自由发挥收紧成"仅限一次性授权兑换的那个动作"。契约里的 credential class(`employment` vs `human-delegation`)就是干这个的。
- **永不建全局大 token**(一个 app 通吃所有岗位所有表):爆炸半径、审计混账、多岗隔离全毁,还主动放弃了飞书按 app 授权的免费边界。
- **读写拆两个凭证类别,联动封路**:给"审批写"这类受控动作单独建 app(凭证只在网关手里),legacy skill 的老 app 收掉写权限只留低风险读。待验证的经验问题(E3 spike):飞书协作者权限能否按"只读/可编辑"拆干净——能拆,封路靠飞书权限模型物理成立;不能拆,该 app 全部调用一起迁进网关。

## 不被飞书绑死:三处盘点

"是不是把应用层带进了核心层?"逐部位盘点,一处已防住、两处要现在钉死:

- **已防住:业务系统(SoR)。** 核心表对业务对象只存"类型 + 不透明 ID",读写全走按类型建的适配器,说明书声明用哪个。契约 ID 长得像 `feishu.bitable.record.update`,但那是注册进目录的**数据**,不是核心代码。硬判据:**核心迁移文件里不许出现 feishu/bitable 字样**,如同不许出现"报销"。
- **要钉死之一:审批通道。** 一次性授权机制本身中立(服务端 nonce 绑人 × 动作 × 单据 × 参数),但实现时若把"确认"写成"发一张飞书卡片",渠道假设就焊进核心。修法:核心定义**渠道能力契约**——可信身份、可送达的确认动作、消息投递三个能力;飞书卡片是最豪华的实现,**Web UI 收件箱是保底实现**:公司一个 IM 都不接,审批、追人、补材料也必须能在 Web 里走完。写进第一批验收。
- **要钉死之二:身份映射(更隐蔽)。** "老张"在飞书是 open_id、在关系表可能是另一个 ID、在 Stella 是一个 user。网关拿"发起人"查"资格"隐含一次跨系统身份翻译,现在默默押在"大家都用飞书、open_id 通吃"上。修法:**"人的身份映射"提成核心一等概念**——Stella 用户 ↔ 各渠道身份 ↔ 各业务系统身份,持久化绑定(渠道侧 linkcode 机制已有),网关只认 Stella 用户,翻译在适配器边界完成。

顺带的发现:**研发助理场景就是现成的"去飞书化"测试**——第二种 SoR(GitHub)加非飞书交互面。它接上网关那天,若授权清单、回执、周报的核心代码一行没改,"没把应用层带进核心"就从主张变成测过的事实。这是研发助理的第三个存在理由(前两个:引擎裁决、dogfood)。

## 分身要建什么?

**几乎不用单独建。**分身 = 现有的个人 agent + 上面同一套档案/权限清单/网关/回执(清单同一张表,区别是分身的行由**本人**签,员工的行由主管签)。真正属于分身的只有三条规则:

1. **默认只有准备权**:清单外的操作,分身把事情准备好,发卡片请本人点确认(复用一次性授权)。
2. **隐私硬边界**:分身的对话和记忆归本人,管理员不可读。现有按人分仓已天然保证,要做的是写进架构测试钉死,永不让步。
3. **永不考核**:分身没有 KPI、不进周报。给分身打分等于给本人装监工,产品红线。

## 哪些是应用,哪些以后再说

**应用实例**(不进核心代码,每个 = 岗位说明书 + 政策文档 + skill):

| 实例             | 存在的目的                                                      |
| ---------------- | --------------------------------------------------------------- |
| 报销专员小星     | 第一个员工,把全部机制跑真                                       |
| HR 招聘专员      | 第二个员工,验证核心代码里没混进报销专用逻辑                     |
| 研发助理(GitHub) | 裁决 Goal/Workflow 引擎值不值得继续投资;兼任去飞书化测试;自己用 |
| 客服知识库       | 组织记忆的第一个重度用户                                        |

分身不需要单独的验证项目——老张用分身提交报销、王经理用分身处理审批,在报销流里自然验证。

**以后再说**(外部客户才需要,第一个员工跑通前不碰):岗位打包安装工具(#766、Pack 安装/升级/回滚)、沙箱运行时重构(#768,多季度迁移,收走 token 改现有注入逻辑就够)、K8s 生产硬化、备份恢复、SSO、运维面板、容量成本。**明确不做**的十二条见 roadmap 第四节。

## 一页总结

```text
先写规则      计件规则、岗位说明书(含契约/谓词/建议授权,全是结构化数据)
再打地基      员工档案 → 权限清单 → 回执表 → 动作网关 → 收走 token
判定的分工    Pack 声明规则(出题) / 网关执行规则(判卷) / 事实留在公司系统里现查
凭证的分工    岗位劳动用岗位 app 的 token / 替人落笔用本人 token 且仅凭一次性授权
让职责完整    巡检、追人、人工任务卡片、周报
让员工成长    成长闭环、组织记忆
用真场景验证  报销(第一个) → HR(证明通用) ;研发助理(兼去飞书化测试)、客服并行
最后才是交付  打包、部署硬化、面板          ← 有外部客户才需要
```

分身全程搭便车:地基建成它就能用,自己只带三条规则(准备权、隐私、不考核)。信任的成长在产品里就是授权清单变长的过程,而每一行的依据都躺在回执表里。
