---
title: 更新日志
description: anna 的所有重要变更记录。
icon: History
---

本文件记录本项目的所有重要变更。

格式基于 [Keep a Changelog](https://keepachangelog.com/en/1.1.0/)，
版本遵循 [语义化版本规范](https://semver.org/spec/v2.0.0.html)。

## [Unreleased]

## [0.17.0] - 2026-04-28

### ✨ 功能

- **管理后台**: 为会话页面添加聊天面板 ([#132](https://github.com/vaayne/anna/pull/132))
- **管理后台**: 重新设计会话页面 UI
- **飞书**: 支持在渠道消息中上传文件 ([#133](https://github.com/vaayne/anna/pull/133))
- **插件**: ManifestBinary 支持高级 mise 后端选项 ([#130](https://github.com/vaayne/anna/pull/130))
- **技能**: 新增 clawhub.ai 技能搜索与安装支持 ([#134](https://github.com/vaayne/anna/pull/134))

### 🐛 Bug 修复

- **Docker**: 将用户 CLI 工具安装在 Docker 缓存层 ([#126](https://github.com/vaayne/anna/pull/126))

### ♻️ 重构

- **Agent**: 将 `internal/agent/runner` 合并入 `internal/agent` ([#131](https://github.com/vaayne/anna/pull/131))

**完整变更日志**：[v0.16.7...v0.17.0](https://github.com/vaayne/anna/compare/v0.16.7...v0.17.0)

## [0.16.7] - 2026-04-27

### 🐛 Bug 修复

- **飞书**: 修复通知工具无法向通过自动注册关联的用户投递消息的问题（union_id 路由使用了错误的 `receive_id_type`）

**完整变更日志**：[v0.16.6...v0.16.7](https://github.com/vaayne/anna/compare/v0.16.6...v0.16.7)

## [0.16.6] - 2026-04-26

### ✨ 功能

- **OAuth**: 扩展飞书/云雀预授权范围，以匹配 lark-cli 域名覆盖（审批、多维表格、日历、通讯录、文档、云盘、消息、邮箱、表格、幻灯片、任务、知识库 — 共 138 个范围）
- **OAuth**: 在渠道聊天中新增飞书/云雀 device-code 流支持

**完整变更日志**：[v0.16.5...v0.16.6](https://github.com/vaayne/anna/compare/v0.16.5...v0.16.6)

## [0.16.5] - 2026-04-25

### 🐛 Bug 修复

- **沙盒**: 要求 Linux 本地沙盒安装 bubblewrap，移除降级方案 ([#129](https://github.com/vaayne/anna/pull/129))
- **沙盒**: 要求 Linux 本地沙盒安装 bubblewrap，移除 unshare/unconfined 降级方案
- **沙盒**: 选择本地沙盒后端时跳过 Docker 预检查
- **沙盒**: 从 bwrap 参数中移除冗余的 `--dir /workspace`
- **沙盒**: 修复 bwrap 绑定顺序 — 在 `--ro-bind / /` 之前先绑定 `/workspace`
- **沙盒**: 强化本地沙盒根目录安全
- **OAuth**: 修复 token 刷新和会话环境注入 ([#128](https://github.com/vaayne/anna/pull/128))

### ✅ 测试

- 当用户命名空间被禁用时跳过依赖 bubblewrap 的测试
- 将测试覆盖率从 51.6% 提升至约 53%
- 在 CI 中安装 bubblewrap 并修复 `omitzero` JSON 标签

### 📝 文档

- 澄清飞书自动配置设置说明

**完整变更日志**：[v0.16.4...v0.16.5](https://github.com/vaayne/anna/compare/v0.16.4...v0.16.5)

## [0.16.4] - 2026-04-24

### 🐛 Bug 修复

- **沙盒**: 默认后端从 Docker 改为本地；插件页面选择现在可正确生效
- **飞书**: 在身份查找前同步运行自动配置

**完整变更日志**：[v0.16.3...v0.16.4](https://github.com/vaayne/anna/compare/v0.16.3...v0.16.4)

## [0.16.3] - 2026-04-24

### 🐛 Bug 修复

- **会话**：`NewSessionID` 改用随机 ID，防止 Docker 容器名称冲突
- **OAuth**：通过 `FlowStore` 持久化 device-code token，而非 broker 实例
- **OAuth**：将 OAuth registry 接入 `PoolManager` 以注入会话环境变量
- **运行时**：上下文取消时提前终止 reconcile
- **运行时**：移除 managed command `Cancel` 中对 `ProcessState` 的竞态读取
- **插件**：添加 `@rsbuild/core` 以满足 `start-plugin-core` 的静态导入要求

### ♻️ 重构

- 应用 `golangci-lint` 的代码检查建议

### 🔧 CI

- 升级 GitHub Actions runner

**完整变更日志**：[v0.16.2...v0.16.3](https://github.com/vaayne/anna/compare/v0.16.2...v0.16.3)

## [0.16.2] - 2026-04-24

### 🐛 Bug 修复

- **插件**：修复默认 mise 表版本未正确设置的问题
- **插件**：隔离 manifest 二进制安装器，防止插件间相互干扰

**完整变更日志**：[v0.16.1...v0.16.2](https://github.com/vaayne/anna/compare/v0.16.1...v0.16.2)

## [0.16.1] - 2026-04-24

### 🐛 Bug 修复

- **插件**：修复 manifest 二进制安装在 mise 拒绝读取不受信任的临时配置文件时失败的问题 — 现在在临时目录上先执行 `mise trust`，再运行 `mise install`

### 🔒 安全

- 解决文档依赖中 9 个 GitHub Dependabot 安全告警

**完整变更日志**：[v0.16.0...v0.16.1](https://github.com/vaayne/anna/compare/v0.16.0...v0.16.1)

## [0.16.0] - 2026-04-24

### ✨ 功能

- **插件**：新增运行时 manifest 工具插件 — 通过 `$ANNA_HOME/plugins.yaml` 支持文件驱动的 CLI 工具集成，含双层合并、二进制供应、基于版本的缓存和管理 UI ([#124](https://github.com/vaayne/anna/pull/124))
- **OAuth**：新增通用 OAuth 注册表/代理框架，集成至运行时、管理端和插件宿主路径；新增 OAuth 提供商的 manifest 校验 ([#125](https://github.com/vaayne/anna/pull/125))
- **插件**：将 `gh`、`lark-cli`、`tap-web` 和 `mise` 统一为一级 CLI 支持工具插件模式，含插件自有二进制、包装器、会话环境、捆绑技能及提示注册 ([#123](https://github.com/vaayne/anna/pull/123))

### ♻️ 重构

- **插件**：整合并简化嵌入式工具和包管理内部逻辑；移除旧版 OAuth 包和未使用的 vault/token 管理代码 ([#125](https://github.com/vaayne/anna/pull/125))
- **认证**：将旧版认证行迁移至 `tool/gh` 和 `tool/lark-cli` 插件；移除旧认证插件包 ([#123](https://github.com/vaayne/anna/pull/123))

**完整变更日志**：[v0.15.0...v0.16.0](https://github.com/vaayne/anna/compare/v0.15.0...v0.16.0)

## [0.15.0] - 2026-04-22

### ✨ 功能

- **密钥库**：新增基于 age 信封加密的用户私密存储，密钥在沙盒会话中自动注入为环境变量 ([#120](https://github.com/vaayne/anna/pull/120))
- **凭据**：统一凭据工具与共享后端服务 ([#122](https://github.com/vaayne/anna/pull/122))
- **OAuth**：为 gh 和 lark-cli 新增设备流 OAuth 认证 ([#121](https://github.com/vaayne/anna/pull/121))
- **工具**：将 GitHub CLI（`gh`）加入预构建沙盒工具包
- **管理界面**：重新设计插件页面，新增标签页导航，拆分账户与凭据 UI

### ⚠️ 破坏性变更

- **沙盒**：移除 `boxsh` 后端 — Docker 现在是唯一的沙盒后端。升级前请安装并启动 Docker Desktop（macOS/Windows）或 Docker 守护进程（Linux）。配置文件中设置 `sandbox.backend = "boxsh"` 的用户，系统将自动将其重新映射并输出一次性日志警告。

### ✨ 改进

- **stdio MCP**：`StartProcess`（stdio MCP 服务器）现在在每次运行时均可正常工作。此前在 `boxsh` 会话中静默失败；Docker 的容器模型为每个会话提供了独立的进程命名空间，stdio MCP 服务器可稳定启动。

### ♻️ 重构

- **沙盒**：简化为仅 Docker 后端 ([#118](https://github.com/vaayne/anna/pull/118))
- **构建**：构建前同步嵌入依赖和系统技能 ([#119](https://github.com/vaayne/anna/pull/119))
- **认证**：将认证配置与密钥库解耦，合并 oauthcli 到凭据模块

### ✅ 测试

- 加强重构覆盖率

**完整更新日志**：[v0.14.0...v0.15.0](https://github.com/vaayne/anna/compare/v0.14.0...v0.15.0)

## [0.14.0] - 2026-04-21

### ✨ 功能

- **资源**：新增内置资源目录和 Agent 模板 ([#117](https://github.com/vaayne/anna/pull/117))
- **飞书**：新增飞书自动配置 ([#116](https://github.com/vaayne/anna/pull/116))

**完整更新日志**：[v0.13.0...v0.14.0](https://github.com/vaayne/anna/compare/v0.13.0...v0.14.0)
