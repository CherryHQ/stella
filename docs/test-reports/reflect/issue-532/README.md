# Reflect #532 Test Reports

This directory stores human-readable evaluation reports for the Reflect #532
candidate generation and evaluation work.

The committed reports summarize curated long-conversation eval runs. Raw per-case
artifacts such as `review_unit.txt`, `result.json`, and generated local harness
outputs are intentionally kept under ignored `dist/reflect-evals/` paths.
