---
title: Telegram 机器人
---

Stella 内置了一个通过长轮询连接的 Telegram 机器人 —— 无需 webhook 或公网 IP。你可以直接在 Telegram 中与 AI 助手对话、发送图片和文档进行分析，也可以在群聊中使用。

## 前提条件

开始之前，请确保你已具备：

- 一个正在运行的 Stella 服务器（`stellad server`）
- 至少在Web UI中配置了一个 AI 提供商（如 Anthropic、OpenAI）
- 一个 Telegram 账号

## 设置

1. 打开 Telegram，向 [@BotFather](https://t.me/BotFather) 发消息创建一个新机器人。保存它给你的 bot token。
2. 如果尚未启动，先启动 Stella 服务器：

   ```bash
   stellad server
   ```

3. 打开Web UI `http://localhost:25678`。
4. 进入 **Channels** 页面，添加一个新的 Telegram 频道实例。
5. 将你的 bot token 粘贴到配置中并保存。
6. 重启 `stellad server` 以激活新频道。

你可以创建多个 Telegram 频道实例（如果你有多个机器人）。每个实例可以在Web UI中绑定到专用的 agent。

所有频道配置（token、允许的 ID、专用 agent 绑定等）都通过Web UI管理。

## 多用户支持

每个 Telegram 用户会从其平台身份自动识别。会话按用户和 agent 分别管理，不同用户拥有各自独立的对话历史。无需手动设置用户。

## 代理切换

你可以使用 `/agent` 命令在可用的 agent 之间切换：

- `/agent` -- 列出所有可用的 agent
- `/agent <name>` -- 切换到指定的 agent

在私聊中，这会设置你的默认 agent。在群组中，这会设置整个群组的活跃 agent。

如果频道实例在Web UI中绑定了专用 agent，则该机器人上的所有聊天都使用绑定的 agent，`/agent` 切换会被禁用。

## 流式响应

机器人实时流式传输 LLM 响应，采用两种策略：

### 私聊：Draft API（Bot API 9.3+）

使用 `sendMessageDraft` 实现流畅的动画流式传输，无需担心速率限制问题。如果 API 不可用，会自动回退到编辑模式。

### 群聊：就地编辑

发送初始消息，并在令牌到达时定期（约每秒）编辑它。流式消息在完成后被删除，然后发送最终响应。

### 工具指示器

当助手执行工具时，你会在流式消息中看到状态指示器：

| 工具     | 指示器 |
| -------- | ------ |
| `bash`   | 闪电   |
| `read`   | 书本   |
| `write`  | 铅笔   |
| `edit`   | 扳手   |
| `search` | 放大镜 |

## 图片支持

你可以在私聊中向机器人发送照片。当你发送图片时：

1. 机器人从 Telegram 下载最高分辨率版本
2. 检测 MIME 类型并编码图片
3. 将其作为多模态消息（图片 + 可选说明）发送给模型
4. 模型分析图片并用文本回复

使用场景：描述截图、分析图表、从照片中读取文档等。

如果模型返回图片（例如来自工具结果），它们会在文本响应后作为 Telegram 照片发送回来。

> **注意：** 图片支持需要支持视觉的模型（例如 Claude 3+、GPT-4o）。

## 文件/文档支持

你可以向机器人发送文档（PDF、DOCX、XLSX 等文件类型）。当你发送文档时：

1. 机器人将文件保存到你的私有 assets 目录
2. 附带 xberg 提取提示传递给 agent，以便读取文件内容
3. 你附加的说明文字会作为你的文本消息一并传入

Agent 随后可使用 `xberg extract` 命令解析文件。

> **注意：** 文件上传需要支持视觉/文档的模型，并且需要为活跃 agent 启用 Xberg skill。

## 群组支持

机器人支持群聊。在群聊中机器人会自动参与：被 @提及时一定会路由给被提及的机器人，其他明确问题在存在可用路由模型时，也可能由 Stella 的语义群聊路由选中回复，否则保持静默。若要让机器人不再参与某个群，把它移出该群即可。

如果希望无 @提及也能语义路由，机器人必须能读取普通群消息。Telegram 中需要在 BotFather 里关闭 bot privacy mode。

## 访问控制

你可以在Web UI中添加允许的用户 ID 来限制哪些 Telegram 用户可以与机器人交互。留空则允许所有用户。在 Telegram 中使用 `/whoami` 命令获取你的用户 ID。

## 通知

机器人同时作为通知后端。你可以在Web UI中配置默认通知聊天和可选的广播频道。

用于：

- `notify` agent 工具（在服务器模式下）
- 调度器任务结果广播

## 模型切换

你可以通过 `/model` 命令触发的内联键盘在对话中切换模型。模型列表支持分页和文本过滤。

## 命令

| 命令                | 描述                                             |
| ------------------- | ------------------------------------------------ |
| `/start` 或 `/help` | 欢迎和帮助信息                                   |
| `/new`              | 压缩会话上下文                                   |
| `/compact`          | 压缩会话上下文                                   |
| `/abort`            | 取消正在进行的响应                               |
| `/model`            | 列出可用模型                                     |
| `/model <number>`   | 按编号切换模型                                   |
| `/model <query>`    | 按名称过滤模型                                   |
| `/agent`            | 列出可用 agent                                   |
| `/agent <name>`     | 切换活跃 agent（私聊中为用户默认，群聊中为群组） |
| `/whoami`           | 显示你的用户 ID                                  |

## 配置参考

以下所有设置都通过Web UI管理。

| 字段          | 描述                                   | 默认值   |
| ------------- | -------------------------------------- | -------- |
| `token`       | Bot API token                          | （必需） |
| `notify_chat` | 用于主动通知的聊天 ID                  |          |
| `channel_id`  | 广播频道（@name 或数字 ID）            |          |
| `allowed_ids` | 允许使用机器人的用户 ID（空 = 所有人） | `[]`     |

## 故障排除

**机器人不响应消息？**

- 确保 `stellad server` 正在运行，且 Telegram 频道已在Web UI中配置。
- 仔细检查 bot token 是否正确。你可以向 [@BotFather](https://t.me/BotFather) 发消息查看你的机器人列表来确认。
- 如果设置了访问控制，请确认你的 Telegram 用户 ID 在 `allowed_ids` 列表中。向机器人发送 `/whoami` 来检查。

**机器人在群组中不响应？**

- 最可靠的触发方式是 @提及机器人。
- 如果你期望无 @提及也能回复，确认至少一个群内 agent 配置了可用于路由的模型，并且消息是明确请求而不是闲聊。
- 确保机器人已被添加到群组中，并有权限读取消息。Telegram 中如果要使用无 @提及路由，需要在 BotFather 里关闭 bot privacy mode。

**图片或文件未被分析？**

- 确保你使用的是支持视觉的模型（如 Claude 3+、GPT-4o）。
- 文件上传需要为活跃 agent 启用 Xberg skill。

**通知不工作？**

- 在Web UI中将 `notify_chat` 字段设置为你希望接收通知的聊天 ID。
- 确保机器人已在该聊天中有过对话。
