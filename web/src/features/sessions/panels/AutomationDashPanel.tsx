// PR 3 (task system v2) deleted the v1 task API. Rewrite pending.
export function AutomationDashPanel() {
  return (
    <div className="p-6 text-sm text-muted-foreground">
      Automation dashboard is being migrated for task system v2.
    </div>
  );
}
